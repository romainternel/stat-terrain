# Regression Guardian — v90 (STORY-43, zones sur le terrain : fondation + Joueurs/Gardiens)

## Portée de ce passage
STORY-43 modifie `renderPlayerDetail()` et `renderGkSheet()` — deux fonctions déjà durcies par des stories précédentes (STORY-30 pour `renderGkSheet()`, layout header terrain/chiffres). Ciblage du risque plausible plutôt qu'un re-test complet :
- Le header de `renderGkSheet()` (STORY-30) a été modifié pour accueillir le nouveau bouton de bascule — risque direct de régression de layout, notamment à largeur étroite (iPhone)
- Les fonctionnalités existantes de `renderPlayerDetail()` non touchées par STORY-43 (sélection d'un tir individuel pour filtrer la grille Impact) mais vivant dans la même fonction modifiée

## Vérifications

| # | Vérification | Résultat |
|---|---|---|
| 1 | Header `renderGkSheet()` à 390px (iPhone) : bouton de bascule + sélecteur GB + bouton plein écran cohabitent sans chevauchement, texte du nom d'équipe ne pousse rien hors cadre | ✅ capture `rg43_gk_narrow_390.png` |
| 2 | Header `renderPlayerDetail()` à 390px : bouton de bascule + "✕ Fermer" cohabitent sans chevauchement avec le nom du joueur (qui passe sur 2 lignes proprement) | ✅ capture `rg43_pd_narrow_390.png` |
| 3 | Fonctionnalité pré-existante non touchée par STORY-43 : clic sur un tir individuel (mode points) filtre toujours la grille Impact sur ce seul tir, `data-pd-clear-sel` apparaît | ✅ "selection active" confirmé par clic réel CDP |
| 4 | `.fs-btn` (plein écran, STORY-30) toujours présent et non recouvert par le nouveau bouton de bascule | ✅ |
| 5 | STORY-43 elle-même (8 zones, marqueur 7m, bascule globale, mode lecteur) | ✅ cf. `docs/qa/QA-43-zones-terrain-fondation-joueurs-gardiens.md` — repris ici comme référence |

## Non re-testé dans ce cycle (hors risque plausible)
Le reste de `renderGkSheet()`/`renderPlayerDetail()` (filtre `S.gkFilter`, filtre `S.gkShotFilter`, sélecteur GB, calculs `gkStats()`) n'est pas modifié par STORY-43 et déjà couvert par les entrées STORY-30/historique de la checklist — aucune raison de le re-tester en profondeur.

## Mise à jour de la checklist
Nouvelle entrée "Bascule points/zones sur le terrain" (STORY-43). Entrée existante "Feuille gardien fusionnée" (STORY-30) complétée avec la re-vérification du header à largeur étroite après l'ajout du bouton de bascule.

## Verdict
**RAS** — aucune régression détectée.
