# Checklist de régression — FENIX Stats

*Maintenue par le Regression Guardian — squad de contrôle BMAD*
*Première initialisation le 2026-07-23, à partir de `CLAUDE.md` et des stories validées par le QA.*

| Feature | Introduite | Critère de bon fonctionnement | Criticité | Dernière vérif. OK |
|---|---|---|---|---|
| Navigation entre les 5 onglets (Équipes/Match/Stats/Bilan/Matchs) | historique | Cliquer un onglet du header bascule vers l'écran correspondant, sur iPad et iPhone | Critique | 2026-07-23 (STORY-18) |
| Saisie d'action en match — mode Expert (workflow complet) | historique, refonte pénalty STORY-32/33 | Sélection action → joueur → terrain → zone de but → validation, sans blocage ; BUT/SAVE/OFF (terrain+zone), PB (auto-validation), PO ouvre l'**encart pénalty sur le terrain** (tireur pré-désigné, BUT/ARRÊT/HC, zone d'impact réelle si `S.trackGK`), PD (assist), 2min (badge→joueur→stat), Annuler ; sélection du GB **réellement actif en jeu** (`S.home.gkId`/`S.away.gkId` via `[data-gk-sel]` sur le scoreboard) distincte du filtre d'affichage Stats (`S.gkFilter`) | Critique | 2026-08-10 (re-testé intégralement par vrais clics lors de v79, après la refonte STORY-32/33 de `clickActionPlayer()`/`validateAndClose()`/`autoValidatePending()`/`selectAction()`/`clickTeam()`/`undoLast()`/`editEvent()`/`deleteEvent()` — BUT/SAVE/OFF complets avec zone, PD, PB, Jet franc, TM, mi-temps, 2min, carton rouge, GB live vs filtre Stats, undo, tous strictement conformes et non affectés par la refonte pénalty ; cf. `docs/regression/v79.md`) |
| Encart pénalty sur le terrain (workflow PO→issue, capture de zone réelle) | STORY-32/33 | Clic joueur sur PO → `PEN_OBT` enregistré immédiatement, encart apparaît (badge MODE PENALTY, tireur, 3 boutons BUT/ARRÊT/HC, ✕ Fermer), `.ml-actions`/toggle POSSESSION grisés et inertes (`is-pen-locked`) ; réassignation du tireur par tap pendant l'étape "3 boutons" ; si `S.trackGK` actif et BUT/ARRÊT tapé → grille de zone 3×3, `goalZone` réellement capturé (jusqu'ici toujours `null`) ; HORS CADRE (ou `trackGK` off) → validation immédiate sans grille ; **4 gardes de robustesse** (`undoLast()`, toggle POSSESSION, `editEvent()`, `deleteEvent()`) ferment/bloquent proprement la transaction en cours, sans exception ni événement orphelin, aux deux sous-étapes ; **P1-4** revalidation du tireur contre le roster courant juste avant l'écriture finale ; **P2-5** activer le mode lecteur sur son propre appareil ferme immédiatement sa propre transaction (`PEN_OBT` déjà enregistré préservé) ; couleurs `PEN_GOAL`/`PEN_SAVE`/`PEN_OFF` distinctes (`--gk-goal`/`--gk-save`/`--gk-off`), sans changer celles de `GOAL`/`SAVE`/`OFF` | Critique | 2026-08-10 (STORY-32/33, Code Review APPROUVÉ, QA PASSED AVEC RÉSERVES — bug Majeur trouvé hors scope de ces deux stories, cf. STORY-37 ; non-régression complète confirmée par le Regression Guardian via CDP en conditions réelles, cf. `docs/regression/v79.md` — ouverture/rendu/réassignation/couleurs/comparatif "Modifier la position" absent-vs-présent, undo aux deux sous-étapes + tap orphelin sur `clickGoalZone()` après undo, fermeture ✕ aux deux sous-étapes, HORS CADRE toujours immédiat, editEvent/deleteEvent bloqués avec le texte d'alerte exact vérifié mot pour mot, P1-4 et P2-5 vérifiés en conditions réelles) |
| Protection Stats → Gardiens contre les zones pénalty (heatmap/compteurs sous SVG) | STORY-34 | `renderGkSheet()` : la heatmap de zone et les compteurs ENCAISSÉS/ARRÊTÉS/H.CADRE sous le terrain SVG **excluent** toujours les événements `isPen` (`PEN_GOAL`/`PEN_SAVE`/`PEN_OFF`), y compris un penalty avec `goalZone` réellement capturé via l'encart (STORY-32) ; `gkStats()`/`gkStatsCombined()` (colonne PEN du détail GB, arrêts/total) **continuent** de les compter séparément, sans changement | Important | 2026-08-10 (STORY-34, Code Review APPROUVÉ, QA PASSED ; non-régression confirmée par le Regression Guardian via CDP — comparaison stricte du HTML rendu de `.gk-sheet-court` **avant/après** l'enregistrement réel d'un nouveau `PEN_GOAL` avec zone via l'encart : identique caractère pour caractère, tandis que `gkStats()` incrémentait bien son `penTotal`/`penGoals` séparément, cf. `docs/regression/v79.md`) |
| Analyse automatique + notes coach éditables depuis Bilan (match archivé) | STORY-35 | Onglet "🧠 Analyse" de Bilan (`matchAnalysis(m)`/`matchExportText(m)`, jumeaux scoped-match de `autoAnalysis()`/`generateExportText()`) : insights et export calculés exclusivement à partir de `m.events`/`m.home`/`m.away`/`m.coachNotes`, jamais `S.*` ; textarea notes (`S.bilanNotesDraft`) éditable et persistée via `dbSaveMatch(m)` avec l'objet match **complet** (jamais de reconstruction partielle qui effacerait événements/scores/effectifs) ; chip dirty-state + confirmation bloquante avant de changer de match avec une note non sauvegardée ; `S.bilanMatch` re-dérivé à chaque rendu (pas de référence obsolète après un aller-retour Matchs/Stats→Bilan) ; isolation stricte bidirectionnelle avec le match en cours (`S.coachNotes` jamais touché) ; mode lecteur désactive la textarea/le bouton Sauvegarder | Important | 2026-08-10 (STORY-35, Code Review APPROUVÉ, QA PASSED ; non-régression confirmée par le Regression Guardian via CDP — isolation testée avec deux marqueurs de texte distincts dans les deux sens, persistance réelle vérifiée via `dbGetAll()` **et** après un vrai rechargement complet de page, robustesse référence stale testée (Bilan→Stats→retour sans re-sélection), brouillon survivant à un `R()` externe déclenché en pleine frappe, confirmation bloquante testée dans les deux sens (Annuler/Accepter) avec le texte exact, cf. `docs/regression/v79.md`) |
| Raccourci PDF depuis Bilan + correctif P0 Realtime (`loadMatchAsCurrent()`) | STORY-36 | Bouton "⚠️ Charger ce match & générer son PDF" (onglet PDF de Bilan) et bouton "📂 Charger" de l'Historique partagent la **même** fonction `loadMatchAsCurrent()` ; **avant tout reset** des données du match archivé, `S.currentMatchId=null` **et** `unsubscribeMatchEvents()` sont exécutés (symétrique à `newMatch()`) — empêche qu'un canal Realtime resté actif n'écrive les données d'un match archivé vers le `matchId` Supabase d'un vrai match en cours affiché sur un autre appareil ; texte de confirmation quantifié (nombre d'événements perdus) uniquement pour le raccourci PDF avec un match en cours non vide, texte simple sinon et pour le bouton Historique (inchangé) ; après acceptation → `S.view="stats"`, `S.statsTab="pdf"` | Critique | 2026-08-10 (STORY-36, Code Review APPROUVÉ, Security Auditor feu vert sans Critique — un Majeur préexistant et hors scope relevé séparément, cf. `docs/security/story-36-loadmatchascurrent.md` ; QA PASSED ; non-régression confirmée par le Regression Guardian via CDP avec un scénario réaliste **complet** — `S.currentMatchId` non-null et canal Realtime réellement créé via `subscribeMatchEvents()` avant le déclenchement du raccourci — `S.currentMatchId` et `realtimeChannel` tous deux vérifiés `null` après coup ; texte quantifié (`S.events.length>0`) vs non quantifié (`===0`) tous deux vérifiés mot pour mot, chemins Annuler (aucun changement d'état) et Accepter (état correctement chargé) testés, cf. `docs/regression/v79.md`) |
| Alertes automatiques (TM conseillé, changez de GB) | historique | Se déclenchent selon les règles documentées dans `CLAUDE.md`, anti-spam 30s | Important | Non re-vérifié dans ce cycle |
| Stats GB (arrêts/total, zones d'impact) | historique | Calculs corrects (`gkStats()`/`gkStatsCombined()`), filtres MT1/MT2 fonctionnels | Important | 2026-08-05 (re-testé lors de v76 — fonctions de calcul non modifiées par STORY-30, valeurs recoupées manuellement conformes, cf. `docs/regression/v76.md`) |
| Export PDF (pagination dynamique) | historique, refonte pagination/contenu STORY-39, zones d'origine + split mi-temps STORY-40/41 | `#gen-pdf-btn` (onglet Stats → PDF) génère un PDF multi-pages (`Page X/N` calculé dynamiquement, plus de nombre de pages fixe) : Comparatif+Top 3 (rang ASCII "1./2./3.", GB qualifié sur base cadrés `gkS.total`/`gkS.pct`), Joueurs (tableaux FENIX+ADVERSAIRE, effectif complet sélectionné, colonnes **MT1/MT2** par joueur), Évolution du score sur sa **propre page** (`ensurePageSpace()` garantit qu'aucune section ne chevauche le pied de page ni n'est coupée quel que soit le volume de données), Carte tir joueur FENIX **et** ADVERSAIRE (une carte par joueur ayant tiré, dernière ligne impaire centrée, section absente si aucun tir, grille Impact **et** grille Origine empilées par carte), Gardiens (Zones d'impact **et** Zones d'origine côte à côte dans la même carte, perspective tireur buts/total, vert/cyan jamais rouge, légende explicite, sans lettres de zone) | Important | 2026-08-13 (STORY-40/41, Code Review APPROUVÉ ×2, QA PASSED ×2 — testé sur 14v14/43 événements (régression pagination/centrage) **et** un jeu dédié à 7 événements couvrant explicitement chacune des 7 zones d'origine (ALG/PVT/ALD/ARG/DC/ARD/AUTRE), classification et rendu confirmés zone par zone ; re-testé par un vrai clic souris CDP sur `#gen-pdf-btn`, cf. `docs/qa/QA-40-zones-origine-tir-pdf.md`, `docs/qa/QA-41-split-mitemps-tableau-joueurs-pdf.md` et `docs/regression/v89.md`) |
| Import/export CSV équipes | historique | Import respecte le format `number,name,position`, export produit un fichier valide | Secondaire | Non re-vérifié dans ce cycle |
| Export/Import "Tout" (historique de matchs, `exportAllMatches`/`importAllMatches`) | historique, filtrage par équipe STORY-50 | "Tout exporter"/"Tout importer" (écran Matchs) — depuis STORY-50, filtré par équipe active (`team_profile`) dans les deux sens : un export ne contient jamais les matchs de l'autre équipe, un import tague les matchs importés avec l'équipe active au moment de l'import (pas un fallback "cf" silencieux) | Important | 2026-08-14 (filtrage ajouté et vérifié dans le cadre de STORY-50, cf. `docs/code-review/STORY-48-50-51.md` — 2 des 3 fuites trouvées en Code Review concernaient cette fonctionnalité) |
| Mode hors-ligne (PWA/service worker) | historique | L'app fonctionne sans réseau après un premier chargement | Critique | Non re-vérifié dans ce cycle (cf. version SW bumpée à v48, à re-tester après un vrai déploiement) |
| Layout Match iPad (paysage/portrait) | historique | Écran Match utilisable sans chevauchement | Critique | 2026-07-23 (capture `03-match-ipad-landscape.png`, avant le fix nav — non affecté par STORY-18) |
| Layout Match iPhone portrait | STORY-02 | Actions/score/timer/terrain utilisables sans chevauchement, ≤430px portrait | Critique | 2026-07-27 (STORY-02, QA PASSED) |
| Layout Match iPhone paysage | STORY-03 | Score/timer/contrôles FENIX visibles sans scroll ; terrain agrandi (scroll encore nécessaire pour l'effectif complet, cf. QA-03) | Critique | 2026-07-27 (STORY-03, QA PASSED WITH NOTES — voir `docs/qa/QA-03-layout-match-iphone-paysage.md`) |
| Navigation header sur iPhone (5 onglets atteignables) | STORY-18 | Tous les onglets atteignables sans être coupés hors écran, portrait et paysage | Critique | 2026-07-23 (STORY-18, QA PASSED) |
| Étiquettes joueurs sur le terrain lisibles (largeur réduite) | STORY-19 | Aucun chevauchement avec un effectif réel (plusieurs joueurs par position) ; effectif complet dégradé mais lisible | Critique | 2026-07-27 (STORY-19, QA PASSED WITH NOTES — zone tactile sous 44px, limite physique assumée, voir `docs/qa/QA-19-chevauchement-joueurs-terrain.md`) |
| Disposition terrain à effectif variable par poste (`courtPlayerPositions()`, ancrage bord + grille) | STORY-38, ancrage bord issu de la session du 11-12/08, grille DC ajoutée STORY-67 | GB empilés verticalement ; ALG/ALD au coin, bord d'encart aligné sur la ligne de touche ; ARG/ARD alignés à 9m, bord sur la touche ; PVT : 1-2 joueurs identique à l'historique, **3 → triangle, 4 → carré** (`spread:"grid"`) ; **DC : même mécanisme `spread:"grid"` avec des pas propres (`hSpread:30, vSpread:10`, adaptés à sa marge verticale réduite en bas de terrain) — 1-4 réutilisent les layouts déjà existants, 5 → disposition dédiée 3 en rangée haute + 2 en rangée basse (STORY-67, corrige le chevauchement des 5 joueurs DC du roster réel FENIX CF)** ; 6+ (tout poste) → grille générique 2 colonnes sans disposition dédiée mais sans exception JS ni sortie du cadre | Critique | 2026-08-12 (STORY-38, Code Review APPROUVÉ, QA PASSED — testé à 3/4/5 pivots sur 3 largeurs de viewport (1600/1000/800px landscape), effectif complet 17 joueurs tous postes, workflow BUT complet par vrais clics jusqu'à validation, cf. `docs/qa/QA-38-terrain-pivots-3-4-joueurs.md` et `docs/regression/v87.md`) ; **2026-08-20 (STORY-67) — grille DC à 5 joueurs vérifiée en vrai navigateur Playwright sur 2 écrans distincts (Match Mode Expert, sélecteur Passe décisive), tablette et iPhone, aucun chevauchement ; régression PVT à 3 joueurs re-testée dans la même session (triangle inchangé) ; clic réel confirmé sur le joueur le plus densément entouré de la grille (coin bas-droit), zone cliquable correctement résolue, cf. `docs/qa/QA-67-68-grille-dc-et-raccourcis-header.md` et `docs/e2e/E2E-67-68-grille-dc-et-raccourcis-header.md`** |
| Cartes Stats/Bilan/Setup avec relief visuel | STORY-04 | `.card`/`.gk-stat` avec dégradé/bordure/ombre cohérents, sans régression de contraste | Important | 2026-07-27 (STORY-04, QA PASSED WITH NOTES — impact visuel confirmé mais jugé insuffisant à lui seul pour "ça claque", voir `docs/qa/QA-04-polish-tokens-ombres-cartes.md`) |
| États interactifs généralisés (active/focus) | STORY-05 | `.nav-b`/`.st-tab` réagissent au tap ; focus clavier visible ; `.act-h`/`.btn` du Match non affectés | Important | 2026-07-27 (STORY-05, QA PASSED — vérifié par vraies touches Tab, pas seulement `.focus()` programmatique) |
| Terrain n'affiche que les joueurs sélectionnés | STORY-20 | Aucun joueur affiché (Match, PD, 2min/carton) tant qu'aucune sélection explicite n'a été faite ; sélection partielle respectée | Critique | 2026-07-28 (STORY-20, QA PASSED — 3 fonctions distinctes testées indépendamment) |
| Numéro manquant affiché discrètement | STORY-21 | Tiret (pas "?") sur le terrain quand un joueur n'a pas de numéro ; `renderTeamSetup` non affecté | Secondaire | 2026-07-28 (STORY-21, QA PASSED) |
| Terrain SVG (fond visuel Match/Stats/PD) | STORY-22, correctif échelle points STORY-46 | Fond de terrain SVG sur les emplacements (Match, Stats Joueurs/Gardiens/Comparaison, mode tir, sélecteurs PD/2min), proportions 6m/9m/7m/4m, référentiel `viewBox 0 0 350 208` préservé ; **mode "points"** : `s.x/.y` (% 0-100) correctement convertis en coordonnées `viewBox` (`/100*350`, `/100*208`) aux 4 sites de rendu (`renderShotOverlay`, `renderPlayerDetail`, `renderGkSheet`, `renderCompareCourt`) — un tir tapé n'importe où sur le terrain s'affiche bien à la même position relative en relecture, plus regroupé dans le coin haut-gauche | Important | 2026-08-13 (STORY-22, QA PASSED WITH NOTES, voir `docs/qa/QA-22-refonte-svg-terrain.md` ; hotfix v92 — STORY-46 : bug de mise à l'échelle du mode "points" trouvé et corrigé, jamais présent en mode "zones" ni dans le PDF, cf. `docs/qa/QA-46-bug-echelle-points-tir-svg.md`) |
| Terrain dimensionné sans scroll forcé sur PC/tablette paysage | STORY-22 (correctif) | Sur écran large paysage (≥700px, hors iPhone paysage ≤932px), le terrain se dimensionne par la hauteur disponible, sans déborder le panneau ni forcer un scroll de page | Important | 2026-07-28 (testé 1366×768, 1440×900, 1920×1080, iPad paysage 1024×768 — aucun débordement ; iPhone paysage 844×390 restauré au comportement d'origine STORY-03 pour éviter un chevauchement d'étiquettes) |
| Mode Simple/Expert : détection, persistance, garde-fou | STORY-23 | Première utilisation → détection par largeur d'écran (iPhone=Simple, sinon Expert) ; un choix explicite déjà enregistré n'est jamais réinitialisé ; toggle fonctionnel sur Équipes et panneau Réglages Match ; confirmation bloquante avant bascule Expert→Simple si des événements existent | Important | 2026-08-03 (re-testé — bascule Expert→Simple→Expert avec événements déjà existants, confirmation acceptée, cf. `docs/regression/v71.md`) |
| Écran Match en mode Simple (saisie rapide par équipe) | STORY-24, PB/Jet Franc ajoutés v99 | BUT/ARRÊT/NON CADRÉ **et désormais PB/JET FRANC** (2e ligne de boutons par équipe, retour direct de Romain — manquants à l'origine) auto-valident sans terrain ni zone via `recordEvent(type,team)` ; score et stats GB corrects ; 2min/Carton R/TM accessibles ; badge de mode visible en continu | Important | 2026-08-14 (PB/Jet Franc vérifiés par appel direct de `recordEvent()` — TURNOVER/FREEKICK correctement enregistrés sans `gkId` (cohérent, pas des tirs) — et par vraie capture d'écran iPhone 390×844 : 2e ligne bien visible et lisible sous BUT/ARRÊT/NON CADRÉ, atteignable au scroll ; re-testé — saisie rapide en mode Simple auto-validée sans terrain, cf. `docs/regression/v71.md`) |
| Carte joueur Équipes (sélection/suppression) | historique, polish STORY-25 | Sélection/désélection et suppression d'un joueur fonctionnelles ; badge numéro, accent d'équipe, état non-sélectionné lisible | Important | 2026-07-29 (STORY-25, QA PASSED — vérifié par vrais clics après le changement de markup) |
| Fondation Supabase (config, sécurité) | STORY-10 | `config.js` chargé, client `sbClient` initialisé ; RLS activée + policies sur `matches`/`match_events` ; inscription publique désactivée ; requête sans authentification refusée | Critique | 2026-07-29 (STORY-10, QA PASSED — vérifié sur le vrai projet Supabase, pas seulement sur le papier, cf. `docs/security/supabase-project-live.md`) |
| Écran d'accès partagé (gate de toute l'app) | STORY-11 | Sans session → écran d'accès, pas d'accès à l'app ; mauvais mot de passe → erreur claire ; déconnexion fonctionnelle ; fail-open si Supabase indisponible ; session persiste après fermeture complète de Safari | Critique | 2026-07-29 (STORY-11, QA PASSED — persistance de session confirmée par Romain sur son propre iPad/iPhone) |
| Synchronisation sortante (outbox) | STORY-12 | Événement créé → mis en file (IndexedDB), persiste au reload, envoi automatique dès réseau/session dispo, idempotent (pas de doublon), jamais bloquant pour la saisie ; annulation propage la suppression | Important | 2026-08-03 (re-testé suite à STORY-15 — `flushOutbox()`/`dequeueEventSync()` modifiés pour passer par `patchSyncIndicator()` ; outbox toujours accessible et alimentée, aucun blocage de la saisie constaté même avec le cycle périodique 15s actif en continu, cf. `docs/regression/v71.md`) |
| Synchronisation entrante (temps réel) | STORY-13 | Abonnement Realtime par match_id, fusion sans doublon (mise à jour en place par id), insertion chronologique correcte, ré-abonnement défensif au retour réseau | Important | 2026-08-03 (re-testé — `mergeRemoteEvent()` insère toujours correctement un événement distant, y compris pendant le mode lecteur, cf. `docs/regression/v71.md`) |
| Reprise de match sur un autre appareil | STORY-14 | Match créé dès "Lancer le match" ; écran de proposition après connexion si match en cours ; reprise restaure équipes/effectifs/GK/période/chrono/événements ; match marqué "finished" à la sauvegarde ou au nouveau match | Important | 2026-07-30 (STORY-14, QA PASSED WITH NOTES — reconstruction vérifiée avec données simulées, scénario réel à deux appareils à confirmer par Romain) |
| Mode lecteur (verrouillage de la saisie, `S.readOnly`) | STORY-26, gardes pénalty étendues par STORY-33 | Bouton du panneau Réglages bascule `S.readOnly` on/off, persistant par appareil (`localStorage hb2_readonly`) ; en mode lecteur, les gardes `if(S.readOnly) return;` bloquent toute écriture locale sur le match en cours (terrain BUT/SAVE/OFF, encart pénalty complet, TM, sanctions 2min/carton, chrono, mi-temps, sélection gardien, undo/delete/edit événement, widget PD, sauvegarde/nouveau match, import CSV match, charger un match depuis l'historique, notes coach Bilan) ; **activer le mode lecteur pendant que sa propre transaction pénalty est ouverte la ferme immédiatement** (`S.actionPanel=null;S.penMode=false;` dans `setReadOnly(true)`, STORY-33 P2-5) plutôt que de la geler à l'écran ; la réception distante (sync entrante Supabase) et le filtre d'affichage Stats (display-only, `S.gkFilter`/`S.gkShotFilter`) restent non affectés, ainsi que l'écran Équipes (hors scope volontaire) ; en mode normal (`readOnly=false`), le parcours de saisie complet reste strictement inchangé | Critique | 2026-08-10 (re-testé intégralement lors de v79 après l'extension STORY-33 — verrouillage confirmé en conditions réelles sur la totalité de la surface d'écriture (sélection d'action, chrono, mi-temps, TM, sanction 2min, sélection GB scoreboard, PD, undo, edit/delete événement — sans même le message d'alerte pénalty puisque la garde `readOnly` passe en premier —, sauvegarde, nouveau match, import CSV match) ; filtre d'affichage Stats→Gardiens vérifié toujours actif en lecture seule ; réception distante vérifiée toujours active via un appel réel à `mergeRemoteEvent()` en mode lecteur (événement inséré avec succès) ; P2-5 (fermeture de sa propre transaction pénalty à l'activation) vérifié en conditions réelles, cf. `docs/regression/v79.md` — RAS) |
| Indicateur de statut de synchronisation (✓ sync / ↻ envoi… / ⚠ hors-ligne) | STORY-15 | Reflète l'état réel dans le bandeau Match (`computeSyncStatus()` : `synced`/`syncing`/`offline`, absent si Supabase non configuré) ; mis à jour par patch DOM ciblé (`patchSyncIndicator()`), jamais par un `R()` complet, depuis les 4 points d'accroche fréquents `flushOutbox()` (appelé après chaque événement ET toutes les 15s en continu pendant tout un match), `dequeueEventSync()` (undo), listeners `online`/`offline` — critère central : un champ texte en cours de saisie ailleurs dans l'app (nom d'équipe, notes coach) ne doit jamais perdre le focus ni son contenu non validé pendant ce cycle | Important | 2026-08-03 (STORY-15, code review APPROUVÉ après correctif — la 1ère passe utilisait un `R()` complet à chaque cycle, corrigée en patch DOM ciblé ; QA PASSED ; non-régression complète confirmée par le Regression Guardian via CDP en conditions réelles, cf. `docs/regression/v71.md` : 33/33 vérifications, dont un test dédié de préservation du focus/valeur sur `team-name-input` et `coach-notes` à travers 2 cycles réels de `setInterval(flushOutbox,15000)` plus un dispatch réel des events `online`/`offline` — aucune perte constatée) |
| Suppression réelle d'un match sur Supabase (`deleteSupabaseMatch`) | STORY-27 | Suppression d'un match sauvegardé depuis l'écran Matchs (`[data-del-match]`) : la ligne `matches` et tous ses `match_events` sont aussi supprimés côté Supabase (ordre `match_events` avant `matches`, contrainte FK sans `ON DELETE CASCADE`) ; suppression **best-effort**, jamais bloquante pour la suppression locale (fire-and-forget, non `await`é) ; matchs sauvegardés avant ce correctif (sans `supabaseMatchId`) ne déclenchent jamais l'appel, sans exception ; texte de confirmation mentionne le caractère définitif et multi-appareil | Important | 2026-08-04 (STORY-27, code review APPROUVÉ, Security Auditor feu vert sans Critique/Majeur nouveau — risque déjà accepté en STORY-10 sur le modèle compte unique, QA PASSED ; non-régression complète confirmée par le Regression Guardian via CDP, cf. `docs/regression/v72.md` : 36/36 vérifications, dont sauvegarde → `supabaseMatchId` persisté correct, suppression d'un match "legacy" sans ce champ (aucune exception, comportement voulu), suppression d'un match avec ce champ (suppression locale immédiate ~0.5s malgré la tentative réseau fire-and-forget), chargement `[data-load-match]` d'un ancien match non affecté, export CSV n'expose pas le champ, import CSV réel via `File`+`change` fonctionne sans erreur — aucune régression sur le reste du parcours critique) |
| Feuille gardien fusionnée (terrain+chiffres) — Stats → Gardiens (`renderGkSheet`) | STORY-30 | Une seule carte par équipe (2 au total), terrain+chiffres visibles ensemble sans scroll, header (nom équipe + sélecteur GB) jamais pris dans le `row-reverse` du terrain/chiffres (bug de layout corrigé en cours de dev) ; dropdown `[data-gk-filter-select]` (`S.gkFilter`, **affichage** Stats uniquement) change chiffres ET terrain ensemble en un seul `change`, sans jamais modifier le gardien réellement actif en jeu (`S.home.gkId`/`S.away.gkId`, piloté séparément par `[data-gk-sel]` sur le scoreboard pendant le match) — les deux sens de l'indépendance sont vérifiés ; label statique si 1 seul GB (couleur d'équipe) ; état vide "Aucun gardien sélectionné" si 0 GB (jamais de `0/0`) ; plein écran (⛶) sans exception ; reset de `S.gkFilter`/`S.gkShotFilter` par `newMatch()` **et** `[data-load-match]` (2 points d'entrée) ; onglet Stats atterrit par défaut sur Comparaison (pas Gardiens) | Important | 2026-08-05 (STORY-30, Code Review APPROUVÉ après correctif — 2 points bloquants visuels initiaux corrigés (tailles responsive 38/34px et 23/20px, dot coloré des chips) —, QA PASSED (9/9 critères) ; non-régression complète confirmée par le Regression Guardian via CDP en conditions réelles, cf. `docs/regression/v76.md` : 58/58 vérifications, dont un test dédié montrant qu'un BUT marqué juste après un changement de GB en direct crédite bien le nouveau gardien en jeu (`event.gkId`) et jamais la valeur de `S.gkFilter` — les deux mécanismes n'ont pas été confondus pendant le développement, confirmé dans les deux sens) | 2026-08-13 (header re-vérifié à 390px après ajout du bouton de bascule points/zones — STORY-43, aucun chevauchement avec le sélecteur GB ni le bouton plein écran, cf. `docs/regression/v90.md`) |
| Bascule points/zones sur le terrain — Stats → Joueurs et Gardiens | STORY-43, recalibrage aile + labels v91 | Bouton `[data-shotview]` (`S.shotViewMode`, persistant par appareil comme `S.mode`, jamais réinitialisé par `newMatch()`/`loadMatchAsCurrent()`, actif même en mode lecteur) bascule entre points localisés (comportement historique) et 8 zones dessinées sur le terrain (AilG/6mG/6mC/6mD/AilD/9mG/9mC/9mD, ratio buts/tirs, plus marqueur `7m` séparé alimenté par `PEN_GOAL`/`PEN_SAVE`/`PEN_OFF`) — réglage global partagé entre tous les écrans concernés, pas un état par écran ; grille Impact (`pd-goalzone`/`goalZoneHeatmap()`) jamais affectée par le bouton ; angle d'aile `COURT_WING_AY/AX` calibré sur un cas réel de tir de coin (pas seulement synthétique) ; labels 6mG/6mD entre les lignes 6m/9m, 6mC entre 4m/6m pour ne jamais chevaucher le badge 7m | Important | 2026-08-13 (STORY-43, Code Review APPROUVÉ, QA PASSED — 8 zones vérifiées une par une avec un jeu de données dédié, cohérence confirmée entre Joueurs et Gardiens (même agrégation partagée), cas gardien sans tir et mode lecteur testés, cf. `docs/qa/QA-43-zones-terrain-fondation-joueurs-gardiens.md` et `docs/regression/v90.md` ; hotfix v91 — corner réel mal classé "6mG" corrigé, cf. addendum dans le même fichier QA et `docs/code-review/STORY-43.md`) |
| Bloc terrain agrégé équipe — Stats → Comparaison (`renderCompareCourt`) | STORY-44, correctif échelle STORY-46 | Deux mini-terrains (FENIX/adversaire) entre le tableau comparatif+évolution et "🎯 Tirs par poste", agrégeant **tous** les tirs de l'équipe (pas un joueur/GB en particulier) ; en-tête `Buts/Tirs` + `PB` par carte ; même bouton de bascule que Joueurs/Gardiens (`S.shotViewMode` partagé) ; mode "points" : marqueurs but/arrêt/hors-cadre **plus** marqueurs PB (losange rouge distinct, `TURNOVER`) ; mode "zones" : 8 zones + 7m, **PB jamais affiché dans les zones** (seulement dans la stat d'en-tête) ; cas 0 événement géré sans exception, affiche "0/0" | Important | 2026-08-13 (STORY-44, Code Review APPROUVÉ, QA PASSED, cf. `docs/qa/QA-44-zones-terrain-comparaison.md` et `docs/regression/v92.md`) |
| Détail joueur en modale compacte — Stats → Joueurs (`renderPlayerDetail`) | STORY-47 | Clic sur 🎯 ouvre une modale centrée (`.overlay`+`.pd-modal`, `width:min(520px,94vw)`, bordure teintée par équipe), **pas** un overlay plein écran ; disposition 2 colonnes (`.gk-sheet-body`/`.gk-sheet-nums`/`.gk-sheet-court`, même format que Gardiens) — BUTS/EFF%/PD/TIRS en colonne chiffres, grille Impact + terrain (points/zones) + légende en colonne terrain ; bascule automatique en colonne unique sous 700px de viewport ; fermeture uniquement via "✕ Fermer" ; sélection d'un tir individuel (`data-pd-shot`) filtre toujours la grille Impact sans affecter les stats d'en-tête ; fonctionne en mode lecteur (vue en lecture, jamais de garde `readOnly`) ; `id="pd-detail-overlay"` (distinct de `id="pd-overlay"` utilisé par le sélecteur de passe décisive `renderPdSelect()` — collision trouvée et corrigée en Code Review) | Important | 2026-08-14 (STORY-47, Code Review APPROUVÉ, QA PASSED — centrage horizontal ET vertical vérifié explicitement sur iPad paysage/portrait et iPhone portrait, interactions testées par vrais clics CDP (ouverture, sélection de tir, fermeture), cas 0 tir et mode lecteur testés, cf. `docs/qa/QA-47-detail-joueur-format-compact.md` et `docs/regression/v93.md`) |
| Rapatriement de l'historique archivé entre appareils (`syncArchivedMatchesIntoLocal`) | STORY-48 | Ouverture de l'écran Matchs déclenche une recherche automatique (une seule fois par chargement de page, `S._historySyncedThisLoad`) des matchs `status='finished'` présents sur Supabase mais absents localement (dédupliqués par `supabaseMatchId`) ; liste locale toujours affichée immédiatement, sans attendre la réponse réseau (fail-open) ; indicateur discret pendant la recherche, toast récapitulatif si des matchs sont récupérés, silence total sinon | Important | 2026-08-14 (STORY-48, Code Review APPROUVÉ, QA PASSED, cf. `docs/qa/QA-48-50-51-deux-equipes-et-sync.md` et `docs/regression/v94.md`) |
| Deux équipes distinctes -18/CF (`S.teamProfile`) — écran de choix + isolation des données | STORY-50/51 | Écran de choix (2 grandes cartes) affiché une seule fois par appareil, mémorisé (`localStorage hb2_team_profile`), changeable depuis les réglages ; effectifs (`hb2_teams_cf`/`hb2_teams_u18`), matchs locaux (IndexedDB, champ `teamProfile`) et matchs Supabase (`team_profile`, y compris les matchs "en cours" resynchronisables) **strictement isolés** entre les deux profils — testé directement en sauvegardant un vrai match sous un profil et en vérifiant son absence totale sous l'autre ; matchs sauvegardés avant cette story traités comme appartenant au profil "cf" (compatibilité) ; export/import CSV et suppression de match tous filtrés par équipe active (3 fuites trouvées et corrigées en Code Review — aucune donnée de l'équipe -18 [mineurs] ne peut fuiter dans un export déclenché depuis le profil CF, et réciproquement) ; Championnat en saisie libre mémorisée séparément par équipe, "Amical" (insensible à la casse) exclu du bilan de saison | Critique | 2026-08-14 (STORY-50/51, Code Review APPROUVÉ après correction de 3 fuites de filtrage, QA PASSED — point de vigilance non vérifiable en CDP headless : rendu `<datalist>` sur iPad Safari réel, à confirmer par Romain ; point de gouvernance R0 [données de mineurs sous le même mot de passe partagé] relayé explicitement, non bloquant mais nécessitant une validation consciente de Romain, cf. `docs/qa/QA-48-50-51-deux-equipes-et-sync.md`, `docs/risks/deux-equipes.md` et `docs/regression/v94.md`) |
| Chrono auto-démarré au lancement du match | STORY-52/M1 | Clic sur "▶ Lancer le match" (écran Équipes) démarre le chrono immédiatement (`S.running=true`) et fixe `S.period=1`, sans clic supplémentaire sur "▶ Start" ; garde anti-double-démarrage de `startTimer()` non affectée | Important | 2026-08-16 (STORY-52, vérifié par vrai clic CDP sur `#launch-match-btn` — `S.running===true`/`S.period===1`/`S.view==="match"` confirmés immédiatement après le clic) |
| Flash de confirmation Mode Simple + possession auto-switch (les deux modes) | STORY-52/M2-M3 | Mode Simple : clic BUT/ARRÊT/NON CADRÉ/PB déclenche un flash visuel bref (`.simple-flash`, ~400ms) sur le bouton cliqué **et** bascule `S.possession` vers l'équipe adverse ; Mode Expert : `.act-h.selected` (mécanisme déjà existant, `--accent`/`--accent-rgb` déjà définies en réalité — diagnostic initial erroné corrigé avant développement) et l'auto-switch de possession de `validateAndClose()` non affectés, tous deux re-vérifiés non régressés | Important | 2026-08-16 (STORY-52, vérifié par vrais clics CDP dans les deux modes — flash visible à l'écran (capture), disparaît après le timeout sans laisser de classe résiduelle, possession bascule bien "home"→"away" sur BUT ; Mode Expert re-testé, `.act-h.selected` toujours fonctionnel avec glow visible) |
| Rappel de mi-temps (toast + bouton MT pulsant) | STORY-52/M4 | À `S.period===1` et `S.time>=1800` (30min), un toast d'alerte apparaît ("pense à basculer sur MT2"), répété toutes les 2min tant que non fait (anti-spam indépendant de `tmLastAlert`) ; bouton `#per-btn` prend la classe `.due` (bordure jaune pulsante) dans ce même état, redevient neutre après la bascule de mi-temps ; déclenché par le tick du chrono, fonctionne identiquement Mode Simple et Expert | Important | 2026-08-16 (STORY-52, `checkHalfTimeReminder()` vérifié directement — toast affiché, `.due` appliqué (capture), anti-spam confirmé sur appel immédiatement répété, reset `S.halfTimeLastAlert=0` au changement de mi-temps vérifié) |
| Bandeau de validation au lancement (GB/effectif manquant) | STORY-53/M5 | À l'arrivée sur l'écran Match, si GB non sélectionné (`S.trackGK` actif) et/ou effectif vide pour une équipe, un bandeau non bloquant liste chaque manque ; `[–]` le réduit vers une pastille près de "⚙ Réglages" (ré-ouvrable), `[✕]` le ferme définitivement pour la session de match ; jamais bloquant pour la saisie ; réinitialisé par `newMatch()` | Important | 2026-08-16 (STORY-53, vérifié par vrais clics CDP — bandeau affiché avec les 2 manques attendus, collapse→pastille visible→reopen→dismiss définitif, tous les états testés dans l'ordre) |
| Écran de lancement dédié — onglet Match sans match actif (`renderMatchLaunch`) | STORY-54 | `!S.currentMatchId && S.events.length===0` → gros bouton "▶ Lancer le match" centré + noms d'équipe + manques (`launchWarnings()`), interface de saisie complète masquée ; bouton retiré d'Équipes ; `loadMatchAsCurrent()` (`currentMatchId=null` mais événements déjà chargés, "📂 Charger"/raccourci PDF STORY-36) et match repris (STORY-14, `currentMatchId` déjà fixé) tous deux affichent l'interface complète directement, jamais l'écran de lancement | Critique | 2026-08-16 (STORY-54, Code Review APPROUVÉ après correction d'un point critique trouvé avant tout test — la garde initiale sur `currentMatchId` seul aurait cassé le chargement d'un match archivé, cf. `docs/code-review/STORY-54.md` — QA PASSED, vérifié par vrais clics CDP sur 2 viewports, cas `loadMatchAsCurrent()` explicitement simulé et confirmé non affecté) |
| Effectif adversaire par défaut (7 postes prêts, `defaultAdversaireTeam()`) | STORY-55 | Profil sans effectif adversaire sauvegardé et chaque `newMatch()` → 7 joueurs (GB/ALG/ARG/DC/PVT/ARD/ALD, nom=poste) déjà sélectionnés, GB assigné ; `newMatch()` repart toujours du modèle générique, jamais de l'effectif (éventuellement renommé) du match précédent ; n'affecte jamais `S.home` | Important | 2026-08-18 (STORY-55, vérifié par CDP — chargement profil neuf conforme, renommage manuel puis `newMatch()` confirmé revenir au modèle générique, capture d'écran de l'onglet Équipes) ; **re-confirmé en navigateur réel (Playwright E2E) le 2026-08-20**, cf. `docs/e2e/E2E-55-61-rosters-scroll-boutons-manuels.md` |
| Effectif FENIX CF réel chargé automatiquement (`defaultFenixCfTeam()`, `FENIX_CF_ROSTER`) | STORY-56 | Profil "cf" sans effectif sauvegardé sur l'appareil → 22 joueurs FENIX Toulouse (noms réels accentués + poste, `selected:false`) chargés une seule fois, jamais réappliqués après une première sauvegarde ; profil "-18" non affecté (aucune liste fournie) ; encodage cp1252 du fichier source (`joueurs.csv`) correctement décodé avant saisie dans le code (accents vérifiés affichés, pas de mojibake) | Important | 2026-08-18 (STORY-56, vérifié par CDP — 22 entrées exactes avec accents corrects (capture d'écran), profil -18 non affecté, persistance après sauvegarde confirmée) ; **re-confirmé en navigateur réel (Playwright E2E) le 2026-08-20** — vrai login production, 22 joueurs affichés sans mojibake, cf. `docs/e2e/E2E-55-61-rosters-scroll-boutons-manuels.md` |
| Position de scroll préservée après chaque re-rendu (`R()`, double-buffer `#app`) | STORY-57 | En paysage tablette/desktop (`#app` conteneur de scroll réel, `body{overflow:hidden}`), sélectionner un joueur en bas d'une longue liste ne remonte plus l'écran en haut ; `window.scrollY` (portrait/téléphone) et `app.scrollTop` (paysage ≥700px) tous deux capturés/restaurés à chaque `R()`, sans conflit entre les deux modes | Important | 2026-08-18 (STORY-57, vérifié par CDP — paysage 1024×700 : scrollTop 1078px préservé après clic sur le dernier joueur d'un effectif de 22 (avant fix : retombait à 0) ; portrait 390×844 : window.scrollY 1631px préservé, non régressé) ; **re-confirmé en vrai navigateur Playwright le 2026-08-20** (paysage 1024×700, clic réel `dispatchEvent` sans l'auto-scroll de l'outil, scrollTop 1096px préservé après re-rendu), cf. `docs/e2e/E2E-55-61-rosters-scroll-boutons-manuels.md` |
| PB/Jet franc taguables sur le terrain en Mode Expert (`clickActionPlayer`/`clickCourtPosition`/`renderMatchPanel`/`renderShotCourt`) | STORY-58 | Sélection PB ou Jet franc → tap joueur → panneau reste ouvert avec surface de tag terrain (`[data-court-position]`), pas de validation instantanée ; tap sur le terrain → événement enregistré avec vraies coordonnées `x`/`y` (jamais `null`), pas de grille "zone de but" affichée (réservée à but/arrêt) ; GOAL/SAVE/OFF non régressés (chemin zone de but toujours affiché pour but/arrêt, tir non cadré toujours sans zone) | Critique | 2026-08-18 (STORY-58, vérifié par CDP vrais clics — PB : `x:30,y:40` capturés, panneau fermé, possession basculée ; Jet franc testé indépendamment : `x:60,y:50` capturés ; bug présent depuis l'introduction du PB, jamais de vraies coordonnées enregistrées avant ce correctif) |
| Surbrillance équipe en possession renforcée (`.ml-team-active`, `.mlt-poss-btn.mlt-poss-active`) | STORY-58 | Bordure 2→3px, fond 9%→18% opacité, glow élargi sur la carte équipe active ; pastille POSSESSION bordure 1.5→2px, fond 15%→22% ; aucun décalage de layout au changement d'épaisseur (`box-sizing:border-box` déjà global) | Secondaire | 2026-08-18 (STORY-58, vérifié visuellement par capture d'écran, contraste net confirmé entre équipe active/inactive) |
| ~~Verrou de possession en Mode Simple~~ (`.simple-inactive`, garde `[data-simple]`) — **⚠️ OBSOLÈTE depuis STORY-65** | STORY-59, rendu sans objet par STORY-65 | ~~Boutons de l'équipe sans `S.possession` grisés mais cliquables ; clic dessus → toast d'erreur~~ — Mode Simple n'affiche plus qu'un seul bloc (celui de l'équipe en possession, STORY-65) : ce scénario ne peut structurellement plus se produire, il n'existe plus de bouton d'une équipe sans possession à cliquer | Critique (historique) | Constat fait le 2026-08-20 pendant l'audit `/verifie-complet` (v113) — ligne conservée pour l'historique, à supprimer ou fusionner avec la ligne "Mode Simple à équipe unique" lors d'un prochain nettoyage de la checklist |
| Rappel GB fiable (`hasValidGk()`, reset `gkId` à la désélection) | STORY-60 | `launchWarnings()` vérifie que le GB assigné est réellement `selected:true`, pas seulement `gkId` non-null ; `defaultFenixCfTeam()` (STORY-56) ne pré-assigne plus de GB fantôme au chargement ; désélectionner le joueur actuellement GB réinitialise `gkId` à `null` (intégrité des stats GB en jeu, pas seulement l'affichage du rappel) | Critique | 2026-08-18 (STORY-60, vérifié par CDP — profil neuf : rappel affiché ; joueurs sélectionnés sans GB explicite : rappel persiste ; GB choisi : rappel disparaît ; GB désélectionné via vrai clic : `gkId` repasse à `null`, rappel réapparaît (capture d'écran de l'écran de lancement)) ; **sous-cas "bandeau affiché avec joueur sélectionné mais sans GB" re-confirmé en vrai navigateur Playwright le 2026-08-20** (login production réel, écran de lancement STORY-54) — sous-cas "disparition après assignation GB" non re-testé en E2E cette fois (nécessite un match actif, hors périmètre retenu pour ne pas écrire sur le Supabase de production), cf. `docs/e2e/E2E-55-61-rosters-scroll-boutons-manuels.md` |
| Alertes TM conseillé / changez de GB en Mode Simple (`recordEvent()`) | STORY-60 | `checkTimeoutAdvisor()`/`checkGkConsecutiveAlert()` désormais appelées aussi depuis `recordEvent()` (Mode Simple), pas seulement `validateAndClose()`/`validateActionPanel()`/`recordTM()` (Mode Expert) — même gap de parité que la possession auto-switch (STORY-52/M3) | Important | 2026-08-18 (STORY-60, vérifié par CDP — 5 buts adverses consécutifs enregistrés via `recordEvent()` en Mode Simple → toast "🚨 5 buts encaissés de suite ! Changez de GB !" déclenché (capture d'écran), ne se déclenchait jamais avant ce correctif) |
| Bouton manuel de rechargement effectif FENIX CF (`[data-load-fenix-cf]`) | STORY-61 | Visible uniquement onglet Équipes, côté FENIX Toulouse, profil "cf" ; absent côté Adversaire et absent profil "-18" ; clic + confirmation → remplace l'effectif par les 22 joueurs officiels (`defaultFenixCfTeam()`), `gkId` remis à `null` | Important | 2026-08-18 (STORY-61, vérifié par CDP — bouton présent/absent testé sur les 2 profils et les 2 côtés, clic confirmé chargeant 22 joueurs avec `Isaac.M` en premier, capture d'écran) ; **re-confirmé en vrai navigateur Playwright le 2026-08-20** — renommage manuel réel (prompt navigateur) puis rechargement confirmé écrasant l'état modifié, cf. `docs/e2e/E2E-55-61-rosters-scroll-boutons-manuels.md` |
| Bouton manuel de rechargement modèle Adversaire (`[data-load-adversaire-template]`) | STORY-61 (addendum) | Visible côté Adversaire uniquement, sur les deux profils (pas spécifique à une équipe FENIX) ; clic + confirmation → remplace l'effectif par les 7 postes (`defaultAdversairePlayers()`), `gkId` remis au nouveau GB, **`S.away.name` jamais modifié** (un nom d'adversaire déjà saisi doit survivre au rechargement) | Important | 2026-08-18 (vérifié par CDP — nom "Ivry" préservé après clic, effectif remplacé par les 7 postes, un seul bouton présent, capture d'écran du placement symétrique avec le bouton FENIX CF) ; **re-confirmé en vrai navigateur Playwright le 2026-08-20** — nom d'équipe personnalisé + joueur renommé, rechargement confirmé restaurant les 7 postes en préservant le nom d'équipe, cf. `docs/e2e/E2E-55-61-rosters-scroll-boutons-manuels.md` |
| Sauvegarde idempotente d'un match (`S.savedMatchId`, `saveMatch()`) | STORY-62 | Sauvegarder deux fois la même session (avec ou sans nouvel événement entre les deux) met à jour la même entrée locale (`id` réutilisé, `dbSaveMatch()` en `put()` upsert), jamais de doublon dans l'historique/Bilan Saison ; `newMatch()` réinitialise `S.savedMatchId=null` (nouvelle sauvegarde au prochain match) ; annuler la confirmation "Nouveau match ?" laisse `S.savedMatchId` intact ; `loadMatchAsCurrent()` fixe `S.savedMatchId` sur l'id du match archivé chargé (le re-sauvegarder met à jour cette même entrée) | Important | 2026-08-20 (STORY-62, vérifié en vrai navigateur Playwright contre le vrai Supabase — sauvegarde ×2 sur la même session : compteur "match(s) en mémoire" inchangé, `id` identique, événements/score à jour ; `newMatch()` puis re-sauvegarde : compteur incrémenté (nouvelle entrée distincte) ; confirmation annulée : `savedMatchId` inchangé ; match archivé rechargé puis modifié puis re-sauvegardé : compteur inchangé, cf. `docs/qa/QA-62-65-corrections-audit-et-mode-simple.md` et `docs/e2e/E2E-62-65-corrections-audit-et-mode-simple.md`) |
| Historique des alertes critiques (`S.alertHistory`, bandeau "🔔 Dernières alertes") | STORY-63 | Toute alerte critique (`showToast(msg,true)`) alimente `S.alertHistory` (3 max, plus récente en premier, horodatée) ; bandeau réductible en pastille `[🔔N]`/fermable, indépendant du bandeau GB (STORY-53) ; `newMatch()` **et** `loadMatchAsCurrent()` réinitialisent l'historique (jamais hérité du match précédent) ; reste visible en mode lecteur (affichage seul, pas une garde d'écriture) | Important | 2026-08-20 (STORY-63, vérifié en vrai navigateur Playwright — 3 alertes réellement déclenchées (buts consécutifs, TM conseillé) affichées et plafonnées à 3, collapse/pastille/reopen testés, reset confirmé après `newMatch()` et après `loadMatchAsCurrent()`, cf. `docs/qa/QA-62-65-corrections-audit-et-mode-simple.md`) |
| Garde-fou volume d'événements sur l'Analyse (`MIN_EVENTS_FOR_INSIGHTS`, `autoAnalysis()`/`matchAnalysis()`) | STORY-64 | Sous 10 tirs cumulés des deux équipes, les insights qualitatifs (efficacité faible, PB, séries, mi-temps, PD, blocs horaires) restent masqués sur les deux écrans (Stats → Analyse **et** Bilan → Analyse) ; résultat + efficacité brute toujours affichés, quel que soit le volume | Important | 2026-08-20 (STORY-64, vérifié en vrai navigateur Playwright — testé pile au seuil : 8 tirs cumulés → seuls 2 insights factuels affichés ; 10 tirs cumulés → tous les insights qualitatifs réapparaissent d'un coup ; même comportement confirmé sur `matchAnalysis()` via un match archivé sous le seuil, cf. `docs/qa/QA-62-65-corrections-audit-et-mode-simple.md`) |
| Mode Simple à équipe unique (`renderMatchSimple()`, `.ml-actions`) | STORY-65 | Un seul bloc de boutons de résultat affiché (celui de l'équipe en possession), plus de duplication par équipe ; nom d'équipe toujours affiché au-dessus des boutons (pas seulement la couleur) ; rangée unique de 5 boutons réutilisant `.ml-actions`/`.act-h` (même conteneur que la barre Mode Expert, déjà responsive) ; bascule automatique après une action et bascule manuelle via "◉ POSSESSION" du scoreboard toutes deux répercutées instantanément ; le verrou de possession STORY-59 (bouton grisé + toast d'erreur) est devenu sans objet et a été retiré, ce cas ne peut plus se produire structurellement | Critique | 2026-08-20 (STORY-65, vérifié en vrai navigateur Playwright — rangée unique confirmée sur iPhone portrait 390×844, iPhone paysage 844×390 et iPad paysage 1024×768 ; bascule de possession testée par clic réel juste après un but, le clic suivant tombe bien sur le bon bouton du bloc redessiné (pas un nœud DOM fantôme) ; mode lecteur testé (0 événement ajouté sur clic) ; cf. `docs/qa/QA-62-65-corrections-audit-et-mode-simple.md` et `docs/e2e/E2E-62-65-corrections-audit-et-mode-simple.md`) |
| Synchronisation d'un match archivé rechargé puis modifié (`queueEventForSync()` → `upsertMatchSnapshot()`) | STORY-66 | Recharger un match archivé (`📂 Charger`, `loadMatchAsCurrent()`) puis ajouter un nouvel événement crée bien la ligne `matches` correspondante (via `upsertMatchSnapshot()`) avant de synchroniser l'événement sur `match_events` — plus d'erreur 409 en boucle ; le flux normal (`▶ Lancer le match`) n'appelle jamais cette branche (aucun appel `upsertMatchSnapshot()` superflu) | Important | 2026-08-20 (STORY-66, vérifié en vrai navigateur Playwright avec inspection directe du journal réseau — `POST /matches` (201) confirmé avant `POST /match_events` (201) sur 2 événements consécutifs après rechargement, ligne `matches` confirmée par requête directe ; flux normal de lancement re-vérifié au journal réseau : toujours exactement 2 `POST /matches` (comportement pré-existant, bouton + `startTimer()`), aucun 3e appel après un événement, cf. `docs/qa/QA-66-fix-sync-match-recharge.md`) |
| Raccourcis en-tête Mode/Suivi GB (`renderHeader()`, `.hdr-shortcuts`) | STORY-68 | Deux raccourcis (Mode Simple/Expert, Suivi GB) toujours visibles dans l'en-tête, sur tous les écrans, à droite du logo et avant les onglets de navigation ; réutilisent `S.mode`/`S.trackGK`/`setMode()` — synchronisés bidirectionnellement avec les emplacements existants (Équipes, panneau Réglages Match) ; la confirmation bloquante Expert→Simple (événements déjà saisis) se déclenche normalement depuis ce raccourci ; sur iPhone étroit (≤700px), le texte "ON"/"OFF" disparaît (icône seule) et les 5 onglets de navigation restent tous atteignables (défilement horizontal STORY-18 non cassé) | Important | 2026-08-20 (STORY-68, vérifié en vrai navigateur Playwright — synchronisation bidirectionnelle testée dans les deux sens (en-tête→Match, Équipes→en-tête), confirmation Expert→Simple testée avec Annuler ET Accepter, en-tête vérifié visuellement dans les 2 états (avec/sans `#settings-btn`) et 2 largeurs (1024px/390px) suite au risque R3 identifié par le Risk Analyst (déplacement de `margin-left:auto`), clics réels confirmés sur les deux raccourcis (pas seulement dispatch), cf. `docs/qa/QA-67-68-grille-dc-et-raccourcis-header.md` et `docs/e2e/E2E-67-68-grille-dc-et-raccourcis-header.md`) |

## Note du Regression Guardian

Cette checklist est neuve — construite rétroactivement à partir de `CLAUDE.md` pour les features historiques (jamais vérifiées dans le cadre de ce squad, seulement documentées) et à partir des stories effectivement passées par le QA pour les entrées récentes. Les lignes "Non re-vérifié dans ce cycle" ne sont pas des échecs — elles indiquent simplement qu'aucun changement de ce cycle ne les touchait, donc pas de raison de les re-tester en profondeur (cf. mon mandat : cibler ce qui est plausiblement à risque, test de fumée sur le reste).

STORY-16 (audit de clarté d'interface) et STORY-17 (documentation de clonage) ne touchent aucun code — rien à ajouter à cette checklist pour elles, conformément au mandat (je maintiens une checklist de comportement de code, pas de documentation).

**v79 (2026-08-10)** : ajout de 4 entrées pour STORY-32/33/34/35/36 (encart pénalty + gardes de robustesse, protection heatmap Gardiens, Analyse/notes coach Bilan, raccourci PDF Bilan + correctif P0 Realtime) et mise à jour des deux entrées historiques directement impactées par la refonte (saisie d'action mode Expert, mode lecteur). Un bug **Majeur pré-existant** a été trouvé par le QA pendant ce cycle (incohérence du calcul "Efficacité %" entre Stats → Analyse du match en cours et Bilan → Analyse d'un match archivé, cf. `docs/stories/STORY-37-bug-efficacite-superieure-100.md`) — hors scope de ce passage de régression : il ne s'agit pas d'une fonctionnalité qui marchait et qui a cassé, mais d'un défaut déjà présent dans `autoAnalysis()`/`teamStat()` avant ce cycle, seulement rendu visible et fréquent par la capture désormais réelle des zones de pénalty (STORY-32). Pas d'entrée checklist créée pour ce bug (mon mandat couvre les critères qui fonctionnent, pas les défauts connus déjà trackés ailleurs) ; à surveiller lors d'un futur cycle qui le corrigera.

**v80-v86 (2026-08-11/12)** : plusieurs correctifs hors-cycle livrés sans passer par ce squad (terrain PDF, marqueurs de tir, homogénéisation Comparatif, chevauchement bouton plein écran, page PDF Joueurs/Cartes de tirs, STORY-37 corrigé) — non repris ici, conformément à CLAUDE.md qui les documente déjà comme tels ; aucune de ces stories n'était passée par un cycle `/construire`+`/verifie` avant STORY-38.

**v87 (2026-08-12)** : ajout d'une entrée pour STORY-38 (disposition terrain à effectif variable par poste, y compris l'ancrage bord ALG/ALD/ARG/ARD/GB déjà livré hors-cycle et jamais entré dans cette checklist jusqu'ici — profité de ce passage pour le documenter en même temps que l'extension PVT 3/4/5). Cf. `docs/regression/v87.md` pour le détail du passage.

**v88 (2026-08-12)** : mise à jour de l'entrée "Export PDF" (obsolète depuis les correctifs hors-cycle v80-v86, jamais reprise jusqu'ici) suite à STORY-39 (pagination robuste + 5 corrections de contenu bundlées : Évolution du score sur sa propre page, glyphe Top 3 ASCII, GB Top 3 sur base cadrés, Carte tir joueur adversaire + centrage, cohérence Zones d'impact avec l'app). Câblage UI réel du bouton PDF (`bind()`/`R()`) vérifié par un vrai clic souris, pas seulement par appel direct de la fonction. Cf. `docs/regression/v88.md`.

**v89 (2026-08-13)** : STORY-40 (grille "Zones d'origine du tir", nouvelle, née de l'évaluation BMAD d'un PDF de référence concurrent partagé par Romain) et STORY-41 (colonnes MT1/MT2 sur le tableau Joueurs) — deux stories indépendantes livrées ensemble, entrée "Export PDF" mise à jour une nouvelle fois plutôt que dupliquée. Un écart de centrage silencieux (`tableX` câblé en dur, non lié au recalcul de largeur des colonnes) a été trouvé et corrigé pendant le Code Review de STORY-41, avant tout passage QA — cf. `docs/code-review/STORY-41.md`. Cf. `docs/regression/v89.md` pour le détail du passage.

**v90 (2026-08-13)** : STORY-43 (fondation de la visualisation "zones sur le terrain" — bouton de bascule points/zones global, 8 zones + marqueur 7m, sur Stats → Joueurs et Gardiens). Modèle de zones lui-même déjà validé en amont par un prototype visuel réel (8 itérations avec Romain, hors de ce squad de contrôle) avant tout code applicatif — cf. `docs/arch/zones-terrain-et-tableau-joueurs.md`. Header de `renderGkSheet()` (STORY-30) re-vérifié à largeur étroite après l'ajout du bouton. Cf. `docs/regression/v90.md`.

**v91 (2026-08-13)** : hotfix ciblé sur STORY-43, déclenché par un retour d'usage réel de Romain (pas un cas de test) — un tir de coin classé "6mG" au lieu de "AilG". Angle d'aile `COURT_WING_AY/AX` recalibré (56/88 → 80/100, un 5e agrandissement après les 4 déjà faits pendant le prototypage — confirme qu'un prototype validé visuellement ne remplace pas un test avec de vraies données), constantes désormais factorisées en une seule paire partagée entre `shotZoneCourt()` et `buildCourtZones()` (corrige une duplication déjà signalée sans être traitée au Code Review initial). Labels 6mG/6mD déplacés entre les lignes 6m/9m, 6mC volontairement laissé entre 4m/6m pour ne pas chevaucher le badge 7m. Aucun autre fichier touché que `app.js`/`sw.js` (bump v90→v91). Cf. addendum dans `docs/qa/QA-43-zones-terrain-fondation-joueurs-gardiens.md` et `docs/code-review/STORY-43.md`.

**v92 (2026-08-13)** : STORY-44 (nouveau bloc terrain agrégé par équipe sur Stats → Comparaison, avec stats Buts/Tirs+PB et marqueurs PB en mode points) **et** STORY-46 (bug pré-existant trouvé pendant la vérification visuelle de STORY-44 : le mode "points" ne convertissait jamais `x`/`y` (%) vers le référentiel `viewBox 350×208`, regroupant tous les tirs dans le coin haut-gauche du terrain sur 3 écrans — Match/aperçu, Joueurs, Gardiens — corrigé aux 4 sites concernés, mode "zones" et export PDF non affectés puisqu'ils convertissaient déjà correctement). Changement de rendu visible sur des matchs déjà enregistrés (tirs "déplacés" à l'écran, aucune donnée modifiée). Cf. `docs/qa/QA-44-zones-terrain-comparaison.md`, `docs/qa/QA-46-bug-echelle-points-tir-svg.md`, `docs/code-review/STORY-44.md`, `docs/code-review/STORY-46.md`.

**v93 (2026-08-14)** : STORY-47 (détail joueur Stats → Joueurs converti d'un overlay plein écran en une modale compacte au format de la carte Gardien, sur retour direct de Romain après usage réel de v92). Une collision d'`id` (`pd-overlay`, déjà utilisé par le sélecteur de passe décisive) a été trouvée et corrigée pendant le développement, avant le Code Review. Centrage horizontal/vertical vérifié explicitement sur 3 viewports (priorité signalée par Romain). Cf. `docs/qa/QA-47-detail-joueur-format-compact.md` et `docs/code-review/STORY-47.md`.

**v94 (2026-08-14)** : STORY-48 (rapatriement automatique de l'historique de matchs entre appareils — un match sauvegardé sur un appareil devient visible sur les autres) + STORY-50/51 (deux équipes distinctes -18/CF dans la même appli — écran de choix mémorisé par appareil, isolation stricte des effectifs/matchs/championnat entre les deux profils ; absorbe STORY-49). Migration SQL (`docs/supabase-migration-season-journee-notes.sql`, colonnes `season`/`journee`/`coach_notes`/`championnat`/`team_profile`) déjà exécutée par Romain. **3 fuites de filtrage par équipe trouvées et corrigées en Code Review** (export/import CSV "Tout", suppression de match) — la plus sérieuse concernait un export CSV pouvant mélanger les données de l'équipe -18 (mineurs) dans un fichier généré depuis le profil CF. **Point de gouvernance R0 signalé explicitement à Romain** (pas seulement documenté) : le modèle de mot de passe partagé unique (déjà en place depuis STORY-10) s'étend maintenant aux données de l'équipe -18, composée de mineurs — non bloquant techniquement, mais nécessite une décision consciente de sa part. Cf. `docs/qa/QA-48-50-51-deux-equipes-et-sync.md`, `docs/code-review/STORY-48-50-51.md`, `docs/risks/deux-equipes.md`.

**v100 (2026-08-16)** : STORY-52 (4 corrections ciblées suite au premier vrai match testé par Romain : chrono auto-démarré, flash de confirmation Mode Simple + possession auto-switch, rappel de mi-temps) + STORY-53 (bandeau de validation au lancement listant GB/effectif manquant, non bloquant). Le mode "équipe générale" (saisie sans joueur attribué), initialement cadré avec STORY-53, a été **abandonné sur demande explicite de Romain** avant développement — seule l'alerte reste, aucun changement sur `ap.shooterId`/`validateAndClose()`/`clickGoalZone()` etc. **Diagnostic initial corrigé pendant le développement** : le brief supposait `--accent`/`--accent-rgb` non définies dans `style.css` (recherche initiale limitée à `app.js`) — en réalité déjà définies (`:root`, ligne 9) et `.act-h.selected` déjà pleinement fonctionnel en Mode Expert, vérifié par capture d'écran avant tout correctif pour éviter un fix inutile. Le vrai manque de confirmation visuelle se trouvait uniquement en Mode Simple (`recordEvent()` enregistre instantanément, sans état "sélectionné" persistant) — un nouveau mécanisme de flash bref (`.simple-flash`, ~400ms, `S.simpleFlash`) a été construit spécifiquement pour ce cas, absent du brief d'origine. Cf. `docs/design/retours-premier-match-reel.md`, `docs/arch/retours-premier-match-reel.md`, `docs/stories/STORY-52-corrections-ciblees-saisie-match.md`, `docs/stories/STORY-53-fenetre-validation-lancement.md`.

**v101 (2026-08-16)** : STORY-54, retour direct de Romain juste après la livraison de v100 ("mettre lancer le match en gros plutôt sur la page match") — le bouton "Lancer le match" quitte l'onglet Équipes pour devenir un écran dédié sur l'onglet Match (confirmé par question de clarification : écran dédié plutôt qu'une bannière au-dessus de l'interface déjà visible). Corrige au passage un trou de contrôle trouvé pendant le développement : avant cette story, l'onglet Match était accessible directement sans passer par "Lancer le match", affichant l'interface de saisie complète sans chrono auto-démarré ni bandeau de validation. **Point critique trouvé et corrigé avant tout test** : la garde initiale sur `!S.currentMatchId` seul aurait cassé le chargement d'un match archivé (`loadMatchAsCurrent()`, "📂 Charger"/raccourci PDF STORY-36, qui met `currentMatchId` à `null` par sécurité P0 tout en chargeant de vrais événements) — corrigée en ajoutant `&& S.events.length===0` à la condition. Cf. `docs/stories/STORY-54-ecran-lancement-page-match.md`, `docs/code-review/STORY-54.md`, `docs/qa/QA-54-ecran-lancement-page-match.md`.

**v102 (2026-08-18)** : STORY-55, retour de Romain ("insérer une équipe Fenix CF automatique plutôt qu'import CSV tout le temps"). En creusant, `joueurs.csv` déposé par Romain à la racine du projet s'est avéré être un modèle intentionnel pour le côté **adversaire** (confirmé par lui : noms = codes de poste, exprès pour lire le poste directement sur le terrain sans renommer) — distinct du besoin FENIX CF réel (nominatif), qui reste en attente de la liste des joueurs. Effectif adversaire par défaut (7 postes, `defaultAdversaireTeam()`) branché sur le premier chargement d'un profil sans effectif adversaire sauvegardé et sur chaque `newMatch()` (repart toujours du modèle générique, jamais de l'ancien effectif éventuellement renommé). Story XS, pas de code-review/QA séparés — vérifié directement par CDP. Cf. `docs/stories/STORY-55-effectif-adversaire-par-defaut.md`.

**v103 (2026-08-18)** : STORY-56, suite immédiate de STORY-55 — Romain a mis à jour `joueurs.csv` avec la vraie liste des 22 joueurs FENIX CF (format `Prénom.InitialeNom`, convention du club). **Piège d'encodage trouvé avant toute saisie** : le fichier est en Windows-1252/ANSI, pas UTF-8 — une première lecture directe produisait des accents mojibake (`Hail�.G`) ; relu explicitement en cp1252 avant de saisir la liste dans `FENIX_CF_ROSTER`, accents corrects vérifiés à l'écran. Chargement automatique au premier lancement sur un appareil sans effectif CF déjà sauvegardé (`defaultFenixCfTeam()`, même schéma que STORY-55 mais `selected:false` par défaut et scopé strictement au profil "cf"), jamais réappliqué après. Story XS, pas de code-review/QA séparés. Cf. `docs/stories/STORY-56-effectif-fenix-cf-par-defaut.md`.

**v104 (2026-08-18)** : STORY-57, retour direct de Romain — la sélection des joueurs à faire jouer (onglet Équipes) remontait tout en haut de l'écran à chaque clic quand il avait défilé bas dans la liste. Root cause : en paysage tablette/desktop, `#app` (pas `window`) est le vrai conteneur de scroll (`body{overflow:hidden}`, cf. STORY-08) — `R()` ne restaurait que `window.scrollY`, jamais `app.scrollTop`, alors que le double-buffer (`cloneNode`+`replaceChild`) remplace `#app` par un nouveau nœud à `scrollTop:0` à chaque rendu. Probablement resté invisible jusqu'ici faute d'un effectif assez long pour déborder en paysage — devenu bien plus fréquent depuis STORY-56 (22 joueurs chargés d'un coup). Fix : capture/restauration de `app.scrollTop` ajoutée à côté de `window.scrollY`, sans risque de conflit entre les deux modes (celui qui n'est pas le vrai conteneur vaut déjà 0). Cf. `docs/stories/STORY-57-scroll-remonte-selection-joueur.md`.

**v105-v106 (2026-08-18)** : STORY-58 (bug PB/Jet franc + surbrillance possession) et STORY-59 (verrou de possession Mode Simple), deux retours consécutifs de Romain dans le même échange. **STORY-58** : le PB (et le Jet franc, même bug) ne pouvait jamais être tagué sur le terrain en Mode Expert — `TURNOVER`/`FREEKICK` avaient `needsMap:true` depuis toujours, mais 4 endroits du code ne vérifiaient que `isGoal||isSave||isOff`, donc validation instantanée au tap joueur sans jamais atteindre l'étape terrain. Confirmé par un commentaire déjà présent dans le code de STORY-44 signalant que les marqueurs PB n'étaient "jamais dessinés" — aucun événement PB n'a donc jamais eu de vraies coordonnées avant ce correctif. Possession également rendue visuellement plus prononcée sur demande explicite. **STORY-59** : en Mode Simple, n'importe quel bouton était cliquable indépendamment de la possession — verrou ajouté (visuel + blocage effectif au clic avec message d'erreur), Mode Simple s'aligne enfin sur la règle déjà appliquée implicitement en Mode Expert. Cf. `docs/stories/STORY-58-tag-terrain-pb-et-surbrillance-possession.md`, `docs/stories/STORY-59-verrou-possession-mode-simple.md`.

**v107 (2026-08-18)** : STORY-60, suite immédiate de STORY-58/59 dans le même échange. Le rappel GB (STORY-53) existait mais ne se déclenchait quasiment plus pour FENIX CF depuis STORY-56 : le roster par défaut pré-assignait un gardien fantôme (non sélectionné pour le match) dès le chargement, et `launchWarnings()` ne vérifiait que la présence d'un `gkId`, jamais sa validité réelle — corrigé aux 3 points coordonnés (plus de pré-assignation prématurée, vérification de `selected:true`, reset automatique à la désélection). Romain a aussi demandé confirmation que les alertes "TM conseillé"/"changez de GB" étaient toujours actives : elles l'étaient dans le code, mais **jamais branchées en Mode Simple** (`recordEvent()`) — même famille de gap de parité que la possession auto-switch de STORY-52/M3, découvert en répondant à sa question plutôt que suite à un test manuel de sa part. Cf. `docs/stories/STORY-60-rappel-gb-et-alertes-mode-simple.md`.

**v108-v109 (2026-08-18)** : STORY-61 — Romain a changé de tablette et le chargement automatique de l'effectif FENIX CF (STORY-56) n'a, pour une raison non confirmable à distance, pas eu lieu sur le nouvel appareil ; aucun bouton n'existait jusqu'ici pour le déclencher manuellement (choix explicite d'origine : "Automatique au premier lancement"). Ajout d'un bouton `⚡ FENIX CF` sur l'onglet Équipes (côté FENIX, profil "cf" uniquement) réutilisant `defaultFenixCfTeam()` — filet de sécurité pour un changement d'appareil futur, sans dépendre d'un diagnostic à distance en plein préparatif de match. Romain a immédiatement demandé le même bouton côté Adversaire ("il me faut idem pour adversaire") — ajouté en addendum (v109), avec une différence volontaire : ne touche jamais `S.away.name`, contrairement à `defaultAdversaireTeam()` utilisée ailleurs, pour ne pas écraser un nom d'adversaire déjà saisi. Cf. `docs/stories/STORY-61-bouton-manuel-effectif-fenix-cf.md`.

**v109 — passage `/verifie` formel a posteriori (2026-08-20)** : STORY-55 à 61 avaient toutes été vérifiées au moment de leur développement (2026-08-18) mais uniquement par moi, en isolation, sans passer par un vrai cycle Code Review → QA → Regression Guardian. Romain a demandé de faire passer ce cycle formellement (`/verifie`, cible clarifiée par question puisque le champ story était vide). Détail complet : `docs/code-review/STORY-55-61.md`, `docs/qa/QA-55-61-cycle-rosters-tag-terrain-possession.md`, `docs/regression/v109.md`. Les 10 lignes ci-dessus (STORY-55 à 61) restent valides sans changement de contenu — ce passage les **confirme** plutôt qu'il ne les modifie, via un scénario combiné qui enchaîne les 7 stories dans l'ordre réel d'usage (pas seulement chacune isolément) pour vérifier leurs interactions. Un point a été trouvé lors du Code Review (non bloquant, cf. `STORY-55-61.md`) : les boutons de rechargement manuel (STORY-61) n'appellent pas `upsertMatchSnapshot()` contrairement au toggle de sélection individuel juste au-dessus dans le code — risque faible (no-op tant qu'aucun match n'est lancé), à corriger par cohérence si l'occasion se présente. Sécurité non convoquée (aucune de ces 7 stories ne touche rôles/auth/ressource backend). E2E non exécuté : pas de serveur MCP Playwright disponible dans cet environnement, étape sautée explicitement plutôt que simulée.

**v109 — addendum E2E réel (2026-08-20, même jour)** : Romain a relancé `/verifie STORY-55-61` ; le serveur MCP Playwright s'est connecté entre-temps, rendant l'étape E2E possible pour la première fois sur ce cycle. Aucun changement de code depuis le passage précédent (`git log` toujours à `63e7b92`, dépôt propre) — seule l'étape E2E, sautée la fois précédente, a été effectivement exécutée. **Décision de périmètre prise avec Romain avant exécution** : l'app n'a pas d'environnement de test séparé (un seul projet Supabase de production, un seul mot de passe partagé) ; Romain a fourni le mot de passe réel pour un vrai login, à condition de ne jamais cliquer "Lancer le match" pendant le test (éviterait de créer un vrai match synchronisé vers ses appareils). STORY-55/56/57/61 (et le sous-cas "bandeau affiché" de STORY-60) confirmés en vrai navigateur Playwright, contre le vrai backend de production, sans aucune écriture Supabase (rosters = localStorage tant qu'aucun match n'est actif). STORY-58/59 et le sous-cas "alerte 5 buts consécutifs"/"disparition du rappel GB" de STORY-60 restent non couverts par un vrai navigateur (nécessitent un match actif) — toujours couverts uniquement par le QA (CDP). Aucune divergence trouvée avec le verdict QA existant. Détail : `docs/e2e/E2E-55-61-rosters-scroll-boutons-manuels.md`.

**v110 (2026-08-20)** : cycle `/verifie` sur STORY-62 à 65, suite directe de l'Audit Final du même jour (`docs/audit-final/AUDIT-2026-08-20.md`) et d'une demande explicite de Romain (Mode Simple à équipe unique). Les 4 stories sont indépendantes (aucune dépendance croisée dans le PRD), livrées et vérifiées ensemble dans un même passage. Cette fois, contrairement à STORY-55-61, un vrai match de test a été créé **et** un match a été réellement lancé (`▶ Lancer le match`) à plusieurs reprises pendant QA et E2E — car STORY-65 (Mode Simple) ne peut pas être vérifiée sans un match actif ; chaque match de test a été entièrement supprimé (local + Supabase, vérifié vide par requête directe) après chaque scénario. **Bug trouvé pendant le QA, hors scope de ces 4 stories** : recharger un match archivé (`📂 Charger`) puis continuer à saisir de nouveaux événements sans relancer le match produit des erreurs 409 en boucle sur `match_events` — `queueEventForSync()` régénère un `currentMatchId` sans jamais appeler `upsertMatchSnapshot()` pour créer la ligne `matches` correspondante (contrairement au bouton de lancement, qui le fait toujours). Confirmé sans lien avec le code de ce cycle (le diff de STORY-62 sur `loadMatchAsCurrent()` ne touche ni `currentMatchId`, ni la synchronisation), un chemin qui semble n'avoir jamais été exercé auparavant. Résidu Supabase (1 ligne `match_events` orpheline) nettoyé manuellement pendant le test, aucune donnée réelle de Romain concernée. Recommandation : nouvelle story dédiée pour ce gap, hors périmètre de ce passage. Détail complet : `docs/code-review/STORY-62-65.md`, `docs/qa/QA-62-65-corrections-audit-et-mode-simple.md`, `docs/e2e/E2E-62-65-corrections-audit-et-mode-simple.md`, `docs/regression/v110.md`.

**v111 (2026-08-20)** : STORY-66, correctif du bug trouvé pendant le QA de v110 (voir ci-dessus). Un seul appel ajouté (`await upsertMatchSnapshot();` dans `queueEventForSync()`), exactement là où l'Architecture l'avait prescrit. Security Auditor et E2E Tester non convoqués — aucune ressource backend nouvelle (réutilise une fonction déjà existante et déjà auditée sur une table déjà couverte par RLS), et aucune surface UI à faire cliquer différemment (correctif de synchronisation invisible pour le coach). QA a confirmé par inspection directe du journal réseau (pas seulement l'absence d'erreur visible) : `POST /matches` (201) précède bien `POST /match_events` (201) sur le chemin corrigé, et le flux normal de lancement continue de ne déclencher que ses 2 appels `upsertMatchSnapshot()` déjà existants (bouton + `startTimer()`), sans appel superflu. Détail complet : `docs/code-review/STORY-66.md`, `docs/qa/QA-66-fix-sync-match-recharge.md`, `docs/regression/v111.md`.

**v112 (2026-08-20)** : STORY-67 (grille DC, corrige le chevauchement des 5 joueurs Demi-Centre du roster réel FENIX CF — le poste le plus peuplé de l'effectif, plus que Pivot qui avait pourtant déjà nécessité une disposition dédiée STORY-38) et STORY-68 (raccourcis Mode/Suivi GB dans l'en-tête, demande directe de Romain), deux retours de Romain traités dans le même cycle. Security Auditor non convoqué pour les deux (aucune ressource backend). Point de vigilance identifié par le Risk Analyst et confirmé traité par le Code Reviewer et le QA : le déplacement de `margin-left:auto` de `#settings-btn` vers le nouveau conteneur `.hdr-shortcuts` (nécessaire pour que les raccourcis restent toujours visibles, pas seulement en match actif) — vérifié sans régression visuelle dans les 2 états et 2 largeurs. E2E a spécifiquement testé le point le plus exigeant de chaque story au clic réel (pas dispatch) : les raccourcis d'en-tête (petits, collés au logo) et le joueur le plus densément entouré de la grille DC — les deux résolus correctement. Détail complet : `docs/code-review/STORY-67-68.md`, `docs/qa/QA-67-68-grille-dc-et-raccourcis-header.md`, `docs/e2e/E2E-67-68-grille-dc-et-raccourcis-header.md`, `docs/regression/v112.md`.

**v113 — audit complet `/verifie-complet` (2026-08-20, même jour)** : passage hors cycle, à la demande de Romain, sur l'intégralité de la checklist Critique+Important (pas le scope d'une story précise). Serveur MCP Playwright disponible, identifiants réels fournis par Romain pour tester en conditions de production (login, sync, mode lecteur, deux équipes) sans se limiter au fail-open local. ~55 features re-testées par clic réel sur `romainternel.github.io/stat-terrain/` : workflow Expert complet (BUT/PB/Jet franc/PO+encart pénalty/2min/carton/TM/undo), Mode Simple à équipe unique (STORY-65) avec alertes TM/Changez-de-GB réellement déclenchées (3, 4, 5 buts consécutifs), historique des alertes, mode lecteur (blocage de clic réel vérifié, pas seulement visuel), 4 viewports (iPad paysage, iPhone portrait/paysage), deux équipes -18/CF avec isolation vérifiée en écriture réelle (`localStorage`), sync sortante confirmée au journal réseau, export PDF réel téléchargé. 0 erreur console inattendue sur ~90 min de session. Match de test créé puis intégralement supprimé de Supabase après usage (vérifié 0 ligne restante) ; jamais sauvegardé en local. **2 bugs Majeurs trouvés, aucun des deux lié à un changement récent** : (1) `loadMatchAsCurrent()` (📂 Charger / raccourci PDF Bilan STORY-36) écrase silencieusement l'effectif courant en `localStorage` par l'instantané du match archivé rechargé — reproduit en réel (effectif CF 22→13 après un "Charger"), aucune mention de l'effectif dans le texte de confirmation ; (2) le bouton "🎯 PD" reste actif après n'importe quel événement (pas seulement un but) et peut attribuer une passe décisive à un carton rouge/2min/TM, corrompant la stat PD sur les 7+ sites d'agrégation qui filtrent uniquement sur `assistId` sans vérifier `isGoal`. Ligne checklist STORY-59 (verrou de possession Mode Simple) signalée obsolète — devenue sans objet depuis STORY-65 (équipe unique affichée). Un match "Yoshi" résiduel (0 événement) trouvé sur Supabase production, probable reliquat non nettoyé d'une session antérieure — signalé, non supprimé par prudence. Rapport complet : `docs/regression/audit-complet-2026-08-20.md`, captures : `docs/regression/screenshots-audit-2026-08-20/`.
