# Regression Guardian — passage avant mise en production v58

## Changement de cette version
STORY-23 : fondation du mode Simple/Expert — état `S.mode`, détection au démarrage, toggle sur l'écran Équipes et le panneau Réglages du Match. **Aucun changement sur le rendu de l'écran Match** à ce stade (vérifié par le QA : `.ml-actions`/`.court-pick` inchangés quel que soit `S.mode`).

## Features ciblées comme plausiblement à risque
Ce changement ajoute un nouveau bloc sur l'écran Équipes et une nouvelle ligne dans le panneau Réglages — les risques plausibles sont localisés à ces deux zones, pas à l'écran Match (qui n'est pas encore branché sur `S.mode`, cf. STORY-24 à venir).

| Feature | Vérification | Résultat |
|---|---|---|
| Toggle "Suivi gardien & zones de tir" (`S.trackGK`) sur l'écran Équipes | Toujours présent, cliqué et vérifié fonctionnel à côté du nouveau bloc de mode | RAS |
| Panneau Réglages Match (Sauvegarder/Exporter/Importer/Effectifs/Nouveau match/Fermer) | Tous les boutons existants toujours présents et cliquables, nouvelle ligne insérée entre Effectifs et Nouveau match sans décaler le reste | RAS |
| Écran Match (workflow complet Expert) | Non affecté par cette story — `S.mode` n'est lu par aucun code de rendu du Match pour l'instant | RAS (rien à re-tester en profondeur ici, ce sera le focus de la régression STORY-24) |
| Bouton "▶ Lancer le match" (Équipes → Match) | Toujours fonctionnel, couleur restaurée après une faute de frappe corrigée en cours de développement | RAS |

## Features non re-testées (hors risque plausible)
Export PDF, import/export CSV, mode hors-ligne, Stats/Bilan — aucun lien avec l'ajout d'un toggle de mode non encore branché sur la logique de saisie.

## Point de vigilance pour la prochaine version (STORY-24)
Le risque P0 identifié par le Risk Analyst ("zéro régression du mode Expert") devient pertinent **à partir de STORY-24**, quand l'écran Match sera réellement branché sur `S.mode`. Le prochain passage de régression devra rejouer explicitement le parcours Expert complet (BUT/SAVE/OFF/PB/PO/PEN/PD/2min/carton, terrain, zones), pas seulement un test de fumée.

## Verdict
**RAS** — aucune régression détectée sur les zones plausiblement impactées par STORY-23.
