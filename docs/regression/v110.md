# Regression Guardian — v110 (STORY-62 à 65 : corrections Audit Final + Mode Simple à équipe unique)

## Contexte
Cycle `/verifie` complet sur les 4 stories issues de l'Audit Final du 2026-08-20 et de la demande directe de Romain sur le Mode Simple. `sw.js` passé en v110 lors de leur implémentation.

## Ce qui a été fait dans ce passage
1. **Developer** : implémentation déjà réalisée et poussée (commit `85a3ecb`) avant ce cycle formel, vérifiée en navigateur réel par le Developer lui-même.
2. **Code Reviewer** : `docs/code-review/STORY-62-65.md` — APPROUVÉ, aucun finding Bloquant ni Recommandé, seulement 2 Notes mineures.
3. **Security Auditor** : non convoqué — aucune des 4 stories ne touche rôles, authentification, ou n'introduit de nouvelle ressource backend (id de sauvegarde local, historique d'alertes en mémoire, seuil d'analyse local, rendu UI local).
4. **QA** : `docs/qa/QA-62-65-corrections-audit-et-mode-simple.md` — PASSED. Scénarios volontairement complémentaires à ceux déjà testés par le Developer (reset par `newMatch()`, annulation de confirmation, mise à jour d'un match repris, bascule manuelle de possession, mode lecteur, paysage iPhone).
5. **E2E Tester** : `docs/e2e/E2E-62-65-corrections-audit-et-mode-simple.md` — CONFIRMÉ. Parcours critique rejoué avec de vrais clics souris (pas de dispatch programmatique), y compris un clic réel juste après un re-rendu complet du bloc Mode Simple (bascule de possession) — aucun décalage détecté.
6. **Regression Guardian** (ce document) : mise à jour de la checklist (5 nouvelles lignes : sauvegarde idempotente, historique alertes, garde-fou Analyse, Mode Simple équipe unique) + vérification ciblée des interactions avec les fonctionnalités existantes.

## Ce qui a été spécifiquement testé dans ce passage (au-delà de ce qui existait déjà)
- **STORY-62 × historique multi-match** : sauvegarder, créer un nouveau match, sauvegarder à nouveau — confirmé que chaque session obtient bien sa propre entrée (pas de fusion accidentelle entre deux matchs différents faute de reset).
- **STORY-62 × STORY-14/36 (reprise/chargement de match)** : `loadMatchAsCurrent()` fixe désormais `S.savedMatchId` — vérifié que ça n'interfère pas avec la garde P0 déjà en place (`S.currentMatchId=null`, `unsubscribeMatchEvents()`) : les deux mécanismes (id de sauvegarde locale vs id Supabase du match) restent bien indépendants comme prévu par l'architecture.
- **STORY-63 × STORY-60 (alertes)** : toutes les variantes d'alertes déjà existantes (TM conseillé ×3 formulations, changez de GB ×2 formulations) remontent bien dans l'historique sans qu'aucun site d'appel n'ait eu besoin d'être modifié individuellement — confirme la robustesse du point de centralisation choisi par l'Architecture.
- **STORY-64 × STORY-35 (Bilan/Analyse d'un match archivé)** : le garde-fou est bien synchronisé entre les deux jumeaux `autoAnalysis()`/`matchAnalysis()` — un même match sous le seuil masque les mêmes insights sur Stats → Analyse et Bilan → Analyse.
- **STORY-65 × STORY-52/59/60 (possession, verrou, alertes Mode Simple)** : la suppression du verrou STORY-59 ne casse pas les alertes GB (STORY-60) branchées sur `recordEvent()` — testé en déclenchant une vraie série de buts consécutifs via le nouveau bloc unique, alerte toujours au bon seuil.
- **STORY-65 × mode lecteur (STORY-26)** : le nouveau bloc à équipe unique reste correctement verrouillé en mode lecteur (0 événement enregistré sur clic testé), sans garde dupliquée ajoutée par erreur.

## Bug trouvé — hors scope, pré-existant
Recharger un match archivé (`📂 Charger`) puis continuer à saisir de nouveaux événements sans relancer le match produit des erreurs 409 en boucle côté Supabase (`match_events`) — `queueEventForSync()` régénère un `currentMatchId` sans jamais appeler `upsertMatchSnapshot()`, contrairement au bouton de lancement. Détail complet et confirmation qu'il est sans lien avec le code de ce cycle : `docs/qa/QA-62-65-corrections-audit-et-mode-simple.md`. Pas ajouté à la checklist de régression (mon mandat couvre les critères qui fonctionnent, pas les défauts nouvellement découverts déjà trackés ailleurs) — à reprendre dans une future story dédiée.

## Vérification de non-fuite / non-régression
Recherche exhaustive confirmée (Code Review + QA) : aucun autre point du code ne dépendait de l'ancien format `data-simple="team|type"` avant sa simplification en `type` seul ; la classe CSS `.simple-inactive` retirée n'est référencée nulle part ailleurs. Le garde `S.readOnly` déjà présent dans `recordEvent()` couvre le nouveau bloc Mode Simple sans modification nécessaire.

## Régressions détectées
Aucune.

## Verdict
**RAS — PRÊT POUR MISE EN PRODUCTION** (déjà en production depuis le déploiement du commit `85a3ecb`, `sw.js` v110 ; ce passage confirme la qualité du code déjà livré et ne bloque rien).
