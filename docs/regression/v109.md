# Regression Guardian — v109 (passage `/verifie` formel sur STORY-55 à 61)

## Contexte
STORY-55 à STORY-61 (effectifs par défaut, tag terrain PB/Jet franc, surbrillance possession, verrou de possession Mode Simple, rappel GB fiable, alertes Mode Simple, boutons manuels de rechargement) avaient été livrées et vérifiées individuellement au moment de leur développement (2026-08-18), directement par CDP, sans cycle Code Review/QA/Regression Guardian formel. Romain a demandé de faire passer ce cycle a posteriori (`/verifie`).

## Ce qui a été fait dans ce passage
1. **Developer** : rien à implémenter — les 7 commits (`6940380`..`8955de9`) étaient déjà poussés sur `main`.
2. **Code Reviewer** : revue indépendante du diff complet, `docs/code-review/STORY-55-61.md` — APPROUVÉ, 1 finding Recommandé (non bloquant).
3. **Security Auditor** : non convoqué — aucune de ces 7 stories ne touche rôles, authentification, ou introduit une nouvelle ressource backend (logique locale/UI, chemins Supabase existants réutilisés tels quels).
4. **QA** : `docs/qa/QA-55-61-cycle-rosters-tag-terrain-possession.md` — PASSED, 14 assertions vérifiées par un scénario CDP **combiné** (les 7 stories enchaînées dans leur ordre réel d'usage, pas isolément).
5. **E2E Tester** : non exécuté — pas de serveur MCP Playwright disponible dans cet environnement. Sauté explicitement, pas simulé.
6. **Regression Guardian** (ce document) : re-vérification ciblée des interactions entre stories + smoke test des zones adjacentes.

## Ce qui a été spécifiquement testé dans ce passage (au-delà de ce qui existait déjà)
- **Combinaison STORY-56+54+60** : profil CF neuf → 22 joueurs auto-chargés → écran de lancement → rappel GB présent tant qu'aucun gardien sélectionné n'est assigné, disparaît une fois assigné correctement.
- **Combinaison STORY-54+58** : lancement réel (chrono démarré) → PB tagué sur le terrain avec de vraies coordonnées → possession bascule (règle préexistante, confirmée non régressée par le changement `needsMap`).
- **Combinaison STORY-59+60** : bascule vers Mode Simple → bouton sans possession grisé et bloqué → 5 buts consécutifs déclenchent bien l'alerte GB en Mode Simple (confirme que le branchement STORY-60 n'a pas été cassé par le verrou de possession STORY-59 ajouté juste avant dans le même fichier).
- **STORY-61** : les deux boutons manuels testés dans le même run que tout le reste (pas isolément) — aucune fuite d'état entre le rechargement FENIX CF et le rechargement Adversaire.
- **STORY-57** : re-testé isolément en paysage réel (1024×700) après avoir détecté que le premier essai, dans le viewport partagé du scénario combiné (820×1180, portrait), ne sollicitait pas le bon conteneur de scroll — corrigé avant de valider, pas laissé comme un faux positif.

## Vérification de non-fuite (recherche exhaustive, pas supposition)
Le fait que TURNOVER/FREEKICK obtiennent désormais de vraies coordonnées `x`/`y` (STORY-58) aurait pu fuiter dans n'importe quel affichage de "tirs" qui filtrerait trop large. Recherche de tous les usages de `needsMap` et de `isGoal||isSave||isOff` dans `app.js` : tous les points d'agrégation/affichage de tirs réels (détail joueur, PDF, stats GB, comparaison) filtrent explicitement par `isGoal||isSave||isOff` (ou `type==="TURNOVER"` exact pour les marqueurs PB dédiés de STORY-44) — jamais par `needsMap` seul. Le seul endroit qui filtre par `needsMap` large (`teamShots()`) alimente une fonction (`renderShotOverlay()`) confirmée **inatteignable** par tout flux utilisateur réel (son seul point d'écriture non-null ne reçoit jamais un type `needsMap:true` en pratique). Aucune fuite.

## Régressions détectées
Aucune.

## Points non-bloquants reportés (déjà notés en Code Review/QA, pas ré-ouverts ici)
- `upsertMatchSnapshot()` manquant sur les 2 handlers de rechargement manuel STORY-61 (risque faible, no-op tant qu'aucun match actif).
- Vider complètement l'effectif FENIX CF (STORY-56) puis recharger la page réactive le chargement automatique — comportement hérité, pas une régression de ce cycle.

## Verdict
**RAS — PRÊT POUR MISE EN PRODUCTION** (déjà en production depuis le 2026-08-18 ; ce passage confirme rétroactivement la qualité du code déjà livré, ne bloque rien).

## Addendum — E2E réel exécuté (2026-08-20, même jour)
Le serveur MCP Playwright, indisponible lors du passage ci-dessus, s'est connecté peu après. Romain a demandé de relancer `/verifie STORY-55-61` pour en profiter. Aucun code n'a changé entre les deux passages (`git log` toujours à `63e7b92`) — seule l'étape E2E, sautée la première fois, a été effectivement exécutée cette fois-ci, contre le vrai backend Supabase de production (vrai mot de passe fourni par Romain), en excluant délibérément tout clic sur "Lancer le match" pour ne pas créer de vrai match synchronisé vers ses appareils (décision validée avec lui avant exécution).

STORY-55/56/57/61 (les 2 boutons) et le sous-cas "bandeau GB affiché" de STORY-60 confirmés en vrai navigateur, sans écart avec le verdict QA. STORY-58/59 et les sous-cas "alerte 5 buts consécutifs"/"disparition du rappel GB" de STORY-60 restent non couverts par l'E2E (nécessitent un match actif, hors périmètre retenu) — toujours couverts par le QA (CDP) uniquement. Aucune régression détectée, checklist mise à jour (10 entrées annotées). Détail complet : `docs/e2e/E2E-55-61-rosters-scroll-boutons-manuels.md`.

**Verdict inchangé : RAS — PRÊT POUR MISE EN PRODUCTION.**
