# Regression Guardian — v111 (STORY-66 : correctif synchronisation match archivé rechargé)

## Contexte
Correctif du bug trouvé par le QA pendant le cycle `/verifie` de STORY-62-65 (`docs/regression/v110.md`), traité comme sa propre story via un cycle `/construire` puis `/verifie` dédié. `sw.js` passé en v111.

## Ce qui a été fait dans ce passage
1. **Developer** : `await upsertMatchSnapshot();` ajouté dans `queueEventForSync()` (`app.js:459`), exactement conforme à `docs/arch/fix-sync-match-recharge.md`.
2. **Code Reviewer** : `docs/code-review/STORY-66.md` — APPROUVÉ, aucun finding.
3. **Security Auditor** : non convoqué — réutilise une fonction (`upsertMatchSnapshot()`) et une table (`matches`) déjà existantes et déjà auditées, aucun nouveau rôle/auth/ressource backend introduit.
4. **QA** : `docs/qa/QA-66-fix-sync-match-recharge.md` — PASSED. Vérification par inspection directe du journal réseau (`browser_network_requests`), pas seulement l'absence d'erreur visible à l'écran — plus rigoureux que la moyenne pour ce type de correctif invisible.
5. **E2E Tester** : non convoqué — aucune surface UI, rien pour un parcours de clic à vérifier différemment de ce que le QA a déjà confirmé au niveau réseau.
6. **Regression Guardian** (ce document) : ajout d'une ligne dédiée à la checklist + vérification ciblée de non-régression sur le flux de lancement normal.

## Ce qui a été spécifiquement testé dans ce passage
- **Reproduction du bug original puis confirmation du correctif** : match sauvegardé → rechargé → nouvel événement → `POST /matches` (201) confirmé avant `POST /match_events` (201), ligne `matches` vérifiée existante par requête directe. Répété sur un 2e événement consécutif — toujours `201`, aucune répétition d'erreur.
- **Non-régression du flux normal** : lancement d'un match + un événement — journal réseau confirmé identique à avant ce correctif (exactement 2 `POST /matches` pendant le lancement, comportement déjà existant via le bouton et `startTimer()`, aucun 3e appel après l'événement). La nouvelle branche de `queueEventForSync()` ne s'exécute donc jamais dans ce flux, comme prévu.
- **`resumeMatch()` (STORY-14)** : non re-testé en conditions réelles (nécessiterait deux appareils simultanés) — couvert par la lecture de code du Code Reviewer, confirmant que `S.currentMatchId` y est toujours déjà défini sur un id existant avant tout événement, donc la branche corrigée n'y est jamais atteignable.

## Régressions détectées
Aucune.

## Verdict
**RAS — PRÊT POUR MISE EN PRODUCTION** (`sw.js` v111).
