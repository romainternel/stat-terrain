# Rapport de non-régression — v56 (STORY-21)

*Produit par le Regression Guardian — squad de contrôle BMAD*

## Changement livré dans cette version

`app.js` (consolidation `displayNumber()`, 3 sites mis à jour), `style.css` (`.cp-num-missing`), `sw.js` (v55→v56). Voir `docs/code-review/STORY-21.md` et `docs/qa/QA-21-numero-maillot-manquant.md`.

## Analyse de risque de régression

Changement purement cosmétique et localisé (`.cp-num` sur le terrain). Vérifié explicitement que `renderTeamSetup` (qui a son propre "?" pour un nom de joueur non renseigné, logique différente basée sur `p.name`) n'est pas concerné par le diff.

## Ce qui a été ciblé (plausible à risque)

- **Les 3 fonctions modifiées** (`renderMatchPanel`, `renderPdSelect`, `renderPlayerSelect`) : testées avec un roster réaliste (numéros vides, comme le roster par défaut) — comportement correct partout.
- **`renderTeamSetup`** : confirmé non affecté par lecture du diff et du code.
- **STORY-20 (juste avant)** : la consolidation de `dn()` ne touche pas au fallback de roster supprimé par STORY-20 — les deux stories cohabitent sans conflit, vérifié par relecture du diff cumulé.

## Ce qui n'a pas été retesté en profondeur

Saisie d'action, alertes, stats, export, mode hors-ligne, layout iPhone : non concernés.

## Verdict

**RAS** — aucune régression détectée.

## Mise à jour de la checklist

`docs/regression/checklist.md` : nouvelle ligne "Numéro manquant affiché discrètement" ajoutée et cochée.

## Bilan du chantier terrain/joueurs

STORY-20 et STORY-21 (les deux bugs de comportement) sont livrés. Reste **STORY-22** (refonte visuelle du terrain lui-même, remplacement de `COURT_IMG`) — le plus gros morceau du cycle, avec validation visuelle explicite de Romain requise avant clôture (cf. `docs/risks/terrain-joueurs.md`).
