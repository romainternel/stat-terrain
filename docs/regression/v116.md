# Régression — v116 (STORY-71 : confirmation et garde-fous au changement de mi-temps)

*Produit par le Regression Guardian — squad de contrôle BMAD*
*S'appuie sur `docs/code-review/STORY-71.md`, `docs/qa/QA-71-confirmation-changement-mi-temps.md`, `docs/e2e/E2E-71-confirmation-changement-mi-temps.md`*

## Périmètre du changement
Une fonction nouvelle (`switchPeriod()`, ~25 lignes) remplace un handler inline anonyme d'une ligne ; un seul point de liaison modifié (`pb.onclick`). Aucune nouvelle structure de données, réutilise exclusivement des fonctions déjà existantes (`safeConfirm`, `startTimer`, `stopTimer`, `showToast`, `upsertMatchSnapshot`).

## Zones identifiées comme plausiblement à risque
- **Rappel de mi-temps réglementaire (STORY-52/M4, `checkHalfTimeReminder()`, `.due` sur `per-btn`)** : logique de seuil (`S.period===1 && S.time>=1800`) non touchée ; un retour en MT1 avec un temps restauré ≥1800 réactivera normalement le rappel dès reprise manuelle du chrono — comportement attendu, vérifié par lecture de code, non contredit par les tests réels (aucun scénario de test n'a atteint ce seuil, mais le mécanisme lui-même n'a subi aucune modification).
- **Compteur de temps morts (`S.tmUsed`)** : vérifié explicitement en conditions réelles inchangé après un aller-retour MT1→MT2→MT1 (`{mt1:1,mt2:0}` avant et après).
- **Historique des alertes critiques (STORY-63, `S.alertHistory`)** : le nouveau toast gardien s'intègre correctement (1 seule entrée après le passage MT1→MT2, toujours 1 seule après le retour MT2→MT1 — confirme qu'aucune alerte n'est déclenchée dans ce sens).
- **Synchronisation multi-appareil (`upsertMatchSnapshot()`)** : toujours appelée dans les deux branches, comportement de la fonction elle-même inchangé.
- **Mode lecteur (`S.readOnly`)** : garde en première ligne de `switchPeriod()`, position identique à l'ancien handler — non affecté (non re-testé en direct ce cycle, risque jugé nul : garde triviale et déjà exercée par le reste du fichier).
- **`renderScoreboard()` (code mort, découvert lors de STORY-70)** : contient toujours l'ancien handler inline non mis à jour (jamais retouché, puisque jamais appelé) — confirmé sans incidence, cette fonction morte n'est appelée nulle part, vérifié à nouveau par recherche exhaustive.

## Méthode
Test de fumée ciblé, complété par une couverture E2E déjà large sur la zone modifiée (4 combinaisons Annuler/Confirmer × 2 sens, plus un spot-check Mode Simple) — pas de re-passage complet de la checklist, changement trop localisé pour le justifier.

## Verdict
**RAS**

Aucune régression détectée. La seule réserve du cycle (écart maquette/contrainte technique `window.confirm()`) est documentée et jugée correctement compensée par le texte du message, vérifié en conditions réelles par l'E2E Tester — pas une régression, un ajustement de mise en œuvre par rapport à une maquette qui supposait une capacité technique inexistante.

## Statut final
**PRÊT POUR MISE EN PRODUCTION**
