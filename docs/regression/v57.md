# Regression Guardian — passage avant mise en production v57

## Changement de cette version
STORY-22 : remplacement du fond raster `COURT_IMG` par un terrain dessiné en SVG natif (`courtSvgMarkup()`), sur les 7 emplacements existants (Match, Stats Gardiens, mode tir, sélecteurs PD/2min).

## Features ciblées comme plausiblement à risque
Ce changement touche uniquement le **fond visuel** du terrain, pas la logique de sélection/positionnement des joueurs ni les coordonnées d'événements. Les features de la checklist qui affichent un élément *par-dessus* ce fond sont donc les seules plausiblement impactées :

| Feature | Vérification | Résultat |
|---|---|---|
| Terrain n'affiche que les joueurs sélectionnés (STORY-20) | Captures Match (90) et iPhone (94) montrent le roster correctement positionné, sans joueur en trop | RAS |
| Numéro manquant affiché discrètement (STORY-21) | Capture 90 : joueur avec tiret visible normalement, pas de "?" | RAS |
| Étiquettes joueurs sur le terrain lisibles (STORY-19) | Capture 94 (iPhone portrait scrollé) : étiquettes lisibles, pas de chevauchement avec le nouveau fond | RAS |
| Layout Match iPhone portrait/paysage (STORY-02/03) | Capture 93/94 : layout général non affecté, seul le fond du bloc terrain a changé | RAS |
| Saisie d'action en match (workflow complet) | Test fonctionnel réel (clics simulés) : BUT complet enregistré correctement avec le nouveau terrain en place | RAS |
| Stats GB (zones d'impact) | Capture 91 : cartes de tir sur fond SVG, rendu correct | RAS |

## Features non re-testées (hors risque plausible)
Export PDF, import/export CSV, mode hors-ligne, alertes automatiques — aucun lien avec le rendu du terrain, pas de raison de les re-tester pour ce changement (test de fumée jugé inutile ici).

## Verdict
**RAS** — aucune régression détectée sur les features plausiblement impactées. Seul point en suspens : la validation visuelle explicite de Romain (critère de STORY-22, pas une régression).
