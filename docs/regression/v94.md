# Regression Guardian — v94 (STORY-48 sync historique, STORY-50/51 deux équipes -18/CF)

## Portée de ce passage
Le plus large changement d'architecture de la session : introduit un nouveau state fondamental (`S.teamProfile`) qui conditionne le chargement des effectifs, le filtrage de tout l'historique local et distant, et ajoute un tout nouvel écran avant le reste de l'app. Risque principal : que ce nouveau filtre ait été oublié à un point d'écriture/lecture existant, laissant fuiter des données entre les deux équipes — c'est effectivement ce qui s'est produit à 3 endroits, trouvés en Code Review (cf. `docs/code-review/STORY-48-50-51.md`) et corrigés avant cette passe.

## Vérifications

| # | Vérification | Résultat |
|---|---|---|
| 1 | Isolation stricte des matchs locaux entre profils — un vrai match sauvegardé en IndexedDB sous "cf" est absent de la liste filtrée sous "u18" | ✅ testé directement sur `dbGetAll()` + filtre |
| 2 | `exportAllMatches()` ("Tout exporter") ne contient que les matchs de l'équipe active — **la fuite la plus sensible** (données de mineurs), corrigée et re-vérifiée | ✅ filtre présent dans le code livré |
| 3 | `importAllMatches()` tague les matchs importés avec l'équipe active (pas de fallback silencieux vers "cf") | ✅ `teamProfile:S.teamProfile\|\|"cf"` présent à la construction |
| 4 | Suppression d'un match (`[data-del-match]`) — rechargement de `S.matchHistory` après suppression correctement filtré | ✅ |
| 5 | `checkForResumableMatch()` jamais appelée avant qu'un profil soit connu (2 points d'appel : `checkAuthSession()` et `signInShared()`) | ✅ les deux vérifiés, pas seulement le premier trouvé |
| 6 | `newMatch()` : ordre correct (`markMatchFinished()` avant les resets de journée/coachNotes/championnat) — bug trouvé et corrigé pendant le développement | ✅ re-vérifié après correction |
| 7 | Exclusion "Amical" du bilan de saison insensible à la casse | ✅ testé avec "Amical" et "amical" dans le même jeu de données |
| 8 | Migration `hb2_teams` → `hb2_teams_cf` : one-shot, n'écrase jamais une clé déjà migrée | ✅ vérifié par lecture de code |
| 9 | Écran de choix d'équipe : persistance réelle après rechargement complet de la page (pas juste en mémoire de session) | ✅ testé par vrai rechargement CDP |
| 10 | Mode lecteur, bascule Simple/Expert, reprise de match en cours (STORY-14) — non cassés par l'ajout du filtre `team_profile` | ✅ vérifié par lecture de code ciblée, aucune garde existante modifiée |
| 11 | Fonctionnalités historiques déjà couvertes par STORY-43/44/46/47 (zones sur le terrain, bloc Comparaison, détail joueur) — aucun de ces écrans ne dépend de `S.teamProfile` | ✅ aucune régression attendue ni trouvée, code non touché par ce cycle |

## Non re-testé dans ce cycle (hors risque plausible)
Contenu du PDF, calculs de stats joueurs/GB, mode Simple — non touchés par ce cycle, comportement déjà couvert par des cycles antérieurs.

## Point non vérifiable dans cet environnement
`<datalist>` (suggestions Championnat) sur iPad Safari réel — CDP headless ne permet pas de le valider fidèlement. Signalé explicitement à Romain dans le rapport de livraison.

## Mise à jour de la checklist
Nouvelles entrées "Rapatriement de l'historique archivé" (STORY-48), "Deux équipes distinctes -18/CF" (STORY-50/51), "Export/Import 'Tout'" (filtrage ajouté). STORY-49 marquée superseded dans son propre fichier story, absorbée par STORY-50.

## Verdict
**RAS** après correction des 3 fuites de filtrage trouvées en Code Review — aucune régression résiduelle détectée. Point de gouvernance R0 (données de mineurs) à faire valider explicitement par Romain, non bloquant pour le déploiement technique.
