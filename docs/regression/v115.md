# Régression — v115 (STORY-70 : le temps mort arrête le chrono)

*Produit par le Regression Guardian — squad de contrôle BMAD*
*S'appuie sur `docs/code-review/STORY-70.md`, `docs/qa/QA-70-tm-arrete-chrono.md`, `docs/e2e/E2E-70-tm-arrete-chrono.md`*

## Périmètre du changement
Une ligne ajoutée (+2 avec le commentaire) dans `recordTM()` (`app.js`). Aucun autre fichier de logique touché. Surface de risque intrinsèquement minuscule — une seule fonction, un seul appel ajouté vers une fonction déjà existante et déjà utilisée ailleurs (`stopTimer()`).

## Zones identifiées comme plausiblement à risque
- **Compteur de temps morts (`S.tmUsed`, max 2/mi-temps, 3 total)** : chemin de code inchangé, l'ajout se situe strictement après l'incrément existant — non affecté.
- **Rappel de mi-temps (STORY-52/M4, `checkHalfTimeReminder()`)** : déclenché uniquement par le tick de l'intervalle du chrono ; un chrono arrêté plus tôt (pendant un TM) signifie simplement moins de ticks, pas de changement de logique de seuil — non affecté.
- **Alertes TM conseillé / changez de GB (`checkGkConsecutiveAlert()`/`checkTimeoutAdvisor()`)** : toujours appelées **avant** le nouvel appel à `stopTimer()`, ordre d'exécution inchangé — non affecté.
- **Synchronisation multi-appareil (`upsertMatchSnapshot()` via `stopTimer()`)** : fonction déjà en production et déjà auditée (STORY-10/13) ; seul le point d'appel change de fréquence, pas son propre comportement — non affecté, effet de bord positif signalé par le Code Reviewer (l'état pausé se propage désormais aussi aux autres appareils).
- **Mode lecteur (`S.readOnly`)** : garde `if(S.readOnly) return;` toujours en première ligne de `recordTM()`, non déplacée — non affecté.

## Méthode
Test de fumée ciblé plutôt qu'un re-passage complet de la checklist — le changement est trop localisé pour justifier un audit `/verifie-complet`. Le rapport E2E (`docs/e2e/E2E-70-tm-arrete-chrono.md`) sert directement de vérification en conditions réelles de la zone modifiée : TM FENIX/Expert, TM Adversaire/Expert, TM FENIX/Simple, tous les trois avec relecture du DOM après un délai réel (pas seulement l'état du bouton juste après le clic). 0 erreur console sur l'ensemble de la session de test.

## Verdict
**RAS**

Aucune régression détectée. Le changement est isolé, sa seule dépendance externe (`stopTimer()`) est une fonction déjà mature et déjà exercée ailleurs dans le code, et les trois combinaisons équipe/mode plausibles ont toutes été vérifiées par clic réel.

## Statut final
**PRÊT POUR MISE EN PRODUCTION**
