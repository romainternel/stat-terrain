# Regression Guardian — passage avant mise en production v61

## Changement de cette version
STORY-25 : polish visuel de `.player-card` (écran Équipes) — badge numéro, accent d'équipe renforcé, état non-sélectionné plus lisible. Changement CSS/markup pur, aucune touche à la logique de sélection/suppression/édition elle-même.

## Features ciblées comme plausiblement à risque
| Feature | Vérification | Résultat |
|---|---|---|
| Sélection/désélection d'un joueur | Testé par vrai clic sur `[data-sel-player]` | RAS |
| Suppression d'un joueur | Testé par vrai clic sur `[data-del]` | RAS |
| Affichage numéro manquant (STORY-21) | Vérifié : badge affiche "–" via `displayNumber()` réutilisée, pas de régression | RAS |
| Badge Gardien (GB) | Toujours affiché conditionnellement sur `position==="GB"` | RAS |
| Effectif vide (équipe adverse par défaut, STORY-24) | Aucun `.player-card` rendu, pas d'erreur | RAS |

## Features non re-testées (hors risque plausible)
Édition nom/poste, ajout de joueur, import/export CSV — code non touché par cette story (seuls le badge numéro et les classes CSS ont changé), pas de raison de les re-tester en profondeur.

## Verdict
**RAS** — aucune régression détectée. Changement à risque intrinsèquement faible (CSS/markup, pas de logique).
