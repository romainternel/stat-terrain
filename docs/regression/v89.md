# Regression Guardian — v89 (STORY-40 + STORY-41, PDF zones d'origine + split mi-temps)

## Portée de ce passage
Deux stories indépendantes (cf. `docs/risks/zones-origine-tir-et-split-mitemps.md`, pas de dépendance entre elles) livrées ensemble dans cette version : STORY-40 (grille Zones d'origine, pages Gardiens + Carte tir joueur) et STORY-41 (colonnes MT1/MT2, page Joueurs). Toutes deux modifient exclusivement `generatePDF()` — même fonction déjà couverte par l'entrée "Export PDF (pagination dynamique)" de la checklist, mise à jour en v88. Ciblage du risque plausible :
- Le câblage UI du bouton PDF (`#gen-pdf-btn` → `generatePDF`) — refait à chaque story qui touche l'intérieur de cette fonction, cf. précédent v88
- La pagination dynamique (STORY-39) — `scCellH` modifié par STORY-40, `tableX`/`colW` modifiés par STORY-41, deux points de contact directs avec le mécanisme déjà durci

## Vérifications

| # | Vérification | Résultat |
|---|---|---|
| 1 | Clic réel sur `#gen-pdf-btn` (souris CDP) après les deux stories → génération sans exception JS | ✅ `{"saved":true,"pages":5,"error":null}` |
| 2 | Pagination dynamique sur effectif complet (14 FENIX + 14 IVRY, jeu STORY-39) toujours cohérente après `scCellH` 46→62 et `colW` 130→135 : Évolution du score toujours sur sa propre page, Carte tir joueur toujours 6 pages avec dernières cartes impaires centrées des deux côtés, tableau Joueurs toujours centré | ✅ cf. `docs/qa/QA-40-...md` et `docs/qa/QA-41-...md` |
| 3 | STORY-40 elle-même (classification 7 zones, rendu Gardiens + Carte tir joueur, cas gardien/joueur sans données) | ✅ cf. `docs/qa/QA-40-zones-origine-tir-pdf.md` — repris ici comme référence |
| 4 | STORY-41 elle-même (MT1/MT2, centrage) | ✅ cf. `docs/qa/QA-41-split-mitemps-tableau-joueurs-pdf.md` — repris ici comme référence |

## Mise à jour de la checklist
Entrée "Export PDF (pagination dynamique)" (v88) enrichie pour mentionner les zones d'origine et le split mi-temps, plutôt que créer deux entrées séparées pour la même fonction déjà trackée — cohérent avec la façon dont STORY-39 elle-même avait consolidé 5 corrections dans une seule entrée.

## Non re-testé dans ce cycle (hors risque plausible)
Aucun autre écran ni fichier touché par ces deux stories — le reste de la checklist n'a aucune raison d'être re-testé.

## Verdict
**RAS** — aucune régression détectée.
