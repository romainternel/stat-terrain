# Rapport de non-régression — v53 (STORY-04)

*Produit par le Regression Guardian — squad de contrôle BMAD*

## Changement livré dans cette version

`style.css` (tokens d'élévation + `.card`/`.gk-stat` revus pour un vrai contraste sur fond sombre) + bump `sw.js` `v52`→`v53`. Voir `docs/code-review/STORY-04.md` et `docs/qa/QA-04-polish-tokens-ombres-cartes.md`.

## Analyse de risque de régression

`.card` est utilisé 19 fois + `.gk-stat` dans `app.js` — rayon d'impact large en SURFACE (touche Setup, Stats, Bilan, et les cartes encore utilisées sur Match comme `.action-panel`/`.modal` qui n'héritent PAS de `.card` directement, à vérifier). Le changement lui-même reste additif (nouveaux tokens, propriétés visuelles uniquement) — aucune suppression de règle existante, aucun changement de structure.

## Ce qui a été ciblé (plausible à risque)

- **Écrans Stats (Gardiens, Comparaison) et Setup** : capturés et comparés visuellement avant/après. Aucune régression de lisibilité, gain visuel confirmé mais modeste.
- **Écran Match** : `.ml-team`, `.mlt-*`, `.act-h` etc. n'utilisent PAS la classe `.card` (ce sont des classes dédiées avec leur propre traitement déjà en place depuis le chantier visuel initial) — donc non affectés par ce diff. Vérifié par lecture du CSS, pas re-capturé (risque nul, classes distinctes).
- **Refactor DRY en cours de route** (extraction de `--card-bg`) : re-vérifié après coup, rendu identique à la version avant refactor.

## Ce qui n'a pas été retesté en profondeur

Saisie d'action, alertes, stats GB (calculs), export PDF/CSV, mode hors-ligne, navigation, layout iPhone : aucun n'est concerné par ce diff (CSS de présentation uniquement, pas de logique).

## Verdict

**RAS** — aucune régression détectée.

## Mise à jour de la checklist

`docs/regression/checklist.md` : nouvelle ligne "Cartes Stats/Bilan/Setup avec relief visuel" ajoutée, avec la note QA-04 sur le résultat jugé insuffisant à lui seul pour l'objectif "ça claque" de Romain.
