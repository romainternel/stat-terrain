# Regression Guardian — passage avant mise en production v62

## Changement de cette version
STORY-10 : fondation Supabase — `config.js`, client `supabase-js` (CDN), `initSupabaseClient()`. Changement purement additif : nouveau fichier, nouveau script CDN, une fonction jamais appelée par le reste de l'app existante.

## Features ciblées comme plausiblement à risque
| Feature | Vérification | Résultat |
|---|---|---|
| Chargement de l'app (aucun script ne doit bloquer le rendu) | Testé : page se charge, `S.view` correct, aucune exception JS | RAS |
| Mode hors-ligne / service worker | `config.js` ajouté à la précache locale ; CDN `supabase-js` volontairement exclu de la précache atomique (cf. notes Developer STORY-10) pour ne pas risquer l'échec du précache complet en réseau faible | RAS — décision documentée, pas une régression |
| Tout le reste de l'app (Match, Stats, Bilan, Équipes) | Code non touché par cette story (`initSupabaseClient()` est isolée, appelée une fois au chargement, ne modifie aucun état `S`) | RAS |

## Features non re-testées (hors risque plausible)
Tout le reste — cette story n'introduit aucune dépendance nouvelle dans le code existant, seulement une fondation isolée pour les stories suivantes (STORY-11+).

## Verdict
**RAS** — aucune régression détectée. Point de vigilance reporté aux prochaines stories (STORY-11 branchera réellement Supabase sur le comportement de l'app).
