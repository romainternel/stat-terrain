# Regression Guardian — passage avant mise en production v68

## Changement de cette version
STORY-14 : reprise de match sur un autre appareil. Enrichit `upsertMatchSnapshot()` (STORY-12) et touche plusieurs points de l'écran Match (chrono, période, GK, sélection roster) pour maintenir le snapshot à jour.

## Vérification approfondie
| Vérification | Résultat |
|---|---|
| Workflow BUT complet (Expert) | ✅ — score correct après terrain+zone |
| Démarrage/arrêt du chrono (`upsertMatchSnapshot` ajoutée) | ✅ — aucune exception |
| Changement de mi-temps (`upsertMatchSnapshot` ajoutée) | ✅ — aucune exception |
| Les 5 écrans de l'app (Équipes/Match/Stats/Bilan/Matchs) | ✅ — aucune erreur console sur l'ensemble du parcours |
| `newMatch()` (appelle maintenant `markMatchFinished()`) | ✅ — fonctionne normalement, aucune exception |
| Reconstruction d'un match repris (données simulées) | ✅ — équipes, effectifs, période, chrono, événements tous corrects et vérifiés champ par champ |

## Point non couvert (documenté, à faire par Romain)
Scénario réel à deux appareils (créer sur A, reprendre sur B, vérifier la correspondance exacte) — nécessite une vraie session Supabase.

## Verdict
**RAS** — aucune régression détectée malgré les nombreux points d'accroche touchés (chrono, période, GK, roster, démarrage de match).
