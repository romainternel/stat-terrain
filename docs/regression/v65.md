# Regression Guardian — passage avant mise en production v65

## Changement de cette version
STORY-13 : synchronisation entrante temps réel. Ajoute un hook d'abonnement dans `queueEventForSync` (déjà touchée en STORY-12) et un hook de désabonnement dans `newMatch()`.

## Vérifications
| Vérification | Résultat |
|---|---|
| Aucune erreur console au chargement de l'app | ✅ |
| Création d'un événement (mode Simple) déclenche l'abonnement sans erreur | ✅ — `S.currentMatchId` posé, score correct, aucune exception |
| `newMatch()` toujours fonctionnel, désabonnement propre | ✅ — `S.currentMatchId` repasse à `null`, `S.events` vidé, aucune exception |
| STORY-12 (sync sortante) non affectée | ✅ — testé implicitement (création d'événement + score correct passe par le même chemin que STORY-12) |

## Point non couvert (documenté, à faire par Romain)
Test réel à deux appareils/sessions (cœur de cette story) — nécessite la réplication Realtime activée côté Supabase (`docs/supabase-realtime-setup.sql`, absente du setup initial STORY-10, trouvée et corrigée pendant cette story) et deux sessions authentifiées réelles.

## Verdict
**RAS** — aucune régression détectée sur l'app existante.
