# Regression Guardian — v87 (STORY-38, disposition Pivot 3/4 joueurs)

## Portée de ce passage
`courtPlayerPositions()` est un point de passage partagé par 4 écrans (sélection de tireur pendant une action, encart pénalty, sélecteur PD, sélecteur de sanction). STORY-38 y ajoute un nouveau mode de disposition (`spread:"grid"`) sans toucher aux 3 autres branches existantes ni aux 4 sites d'appel. Ciblage du risque plausible plutôt qu'un re-test complet du projet, conformément au mandat :
- Comportements historiques directement adjacents à la fonction modifiée (STORY-19, STORY-20)
- Workflow de saisie complet bout en bout (pas seulement le rendu visuel des positions)

## Vérifications

| # | Vérification | Résultat |
|---|---|---|
| 1 | STORY-20 (terrain vide tant qu'aucun joueur sélectionné) : 1 joueur non sélectionné → message vide affiché, 0 encart joueur rendu | ✅ `hasEmptyMsg:true, playerBoxCount:0` |
| 2 | Workflow complet BUT (poste PVT, 2 joueurs) : clic joueur → terrain → zone d'impact → validation → événement enregistré avec les bonnes données | ✅ 1 événement `GOAL`, `playerId` correct, `x/y/goalZone` corrects, `actionPanel` proprement remis à `null` après validation |
| 3 | STORY-38 elle-même (3/4/5 pivots, 3 largeurs de viewport, effectif complet 17 joueurs) | ✅ cf. `docs/qa/QA-38-terrain-pivots-3-4-joueurs.md` — repris ici comme référence, pas re-testé une 2e fois |

## Non re-testé dans ce cycle (hors risque plausible)
STORY-22 (terrain SVG lui-même, `courtSvgMarkup()`) : fichier/fonction non touchés par STORY-38, aucune raison de re-tester. Encart pénalty (STORY-32/33) et sélecteur PD : consomment `courtPlayerPositions()` mais avec la même logique que le site déjà testé (workflow BUT) — smoke test jugé suffisant plutôt qu'une répétition à l'identique sur chaque site d'appel.

## Verdict
**RAS** — aucune régression détectée.
