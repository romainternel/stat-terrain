# Rapport de non-régression — v50 (STORY-03)

*Produit par le Regression Guardian — squad de contrôle BMAD*

## Changement livré dans cette version

`style.css` (condensation colonne équipe/timer + barre d'actions pour iPhone paysage) + bump `sw.js` `v49` → `v50`. Voir `docs/code-review/STORY-03.md` et `docs/qa/QA-03-layout-match-iphone-paysage.md`.

## Analyse de risque de régression

Changement scopé à `@media (max-width:932px) and (orientation:landscape)`. N'affecte pas l'iPad (>932px), ni le mode portrait (media feature `landscape` exclusif), ni STORY-02 (media feature `portrait` distincte, aucun chevauchement de condition).

## Ce qui a été ciblé (plausible à risque)

- **Écran Match iPad paysage** : mesures DOM identiques avant/après (`.act-h` 71px, `.ml-actions` 85px, boutons de contrôle 34-37px) + capture visuelle identique. RAS.
- **Écran Match iPhone portrait (STORY-03 juste avant)** : aucune classe commune modifiée entre les deux media queries (`orientation:portrait` vs `orientation:landscape`, mutuellement exclusifs) — risque de collision nul par construction.
- **La feature elle-même (paysage iPhone)** : re-testée et mesurée, voir QA-03. PASSED WITH NOTES (amélioration réelle, terrain encore partiellement scrollable).

## Ce qui n'a pas été retesté en profondeur

Saisie d'action, alertes, stats GB, export PDF/CSV, mode hors-ligne, navigation header : aucune de ces zones touchée par le diff (`app.js` non modifié). Cohérent avec le mandat du Regression Guardian.

## Verdict

**RAS** — aucune régression détectée. Le point "PASSED WITH NOTES" du QA (scroll encore nécessaire pour voir tout l'effectif sur le terrain) n'est pas une régression — c'est une amélioration partielle d'un état déjà dégradé, documentée comme telle.

## Mise à jour de la checklist

`docs/regression/checklist.md` : ligne "Layout Match iPhone paysage" passée de ❌ à ✅ avec la nuance QA-03 explicitement rappelée (pas un blanc-seing total, juste une amélioration mesurée et acceptée).

## État global du chantier iPhone (cycle 1)

Avec STORY-18, STORY-02 et STORY-03 livrées, les trois blocages critiques identifiés lors de l'audit initial (nav header inaccessible, actions qui se chevauchent en portrait, terrain/chrono en paysage) sont désormais tous traités. Restent ouvertes : STORY-09 (audit du workflow de saisie, jamais testé en conditions réelles de clic), et la trouvaille annexe sur `.cp-player` (chevauchement des étiquettes joueurs à effectif complet, toutes orientations confondues à largeur réduite) qui n'a pas encore de story dédiée.
