# Regression Guardian — v112 (STORY-67 : grille DC + STORY-68 : raccourcis en-tête)

## Contexte
Deux retours de Romain traités dans le même cycle `/construire` → `/verifie`. `sw.js` passé en v112.

## Ce qui a été fait dans ce passage
1. **Developer** : `POS_XY.DC` reçoit `spread:"grid"` (pas propres 30/10) + nouveau layout à 5 dans `courtPlayerPositions()` (STORY-67) ; `.hdr-shortcuts` toujours rendu dans `renderHeader()`, `margin-left:auto` déplacé depuis `#settings-btn`, 2 nouveaux handlers dans `bind()` (STORY-68).
2. **Code Reviewer** : `docs/code-review/STORY-67-68.md` — APPROUVÉ, bornes de la grille DC revérifiées par calcul indépendant, restructuration flex de l'en-tête vérifiée cohérente.
3. **Security Auditor** : non convoqué pour les deux stories — aucune ressource backend, aucun rôle/auth touché.
4. **QA** : `docs/qa/QA-67-68-grille-dc-et-raccourcis-header.md` — PASSED. Grille DC vérifiée sur 2 écrans distincts + régression PVT ; en-tête vérifié dans les 2 états et 2 largeurs identifiés comme point de risque par le Risk Analyst.
5. **E2E Tester** : `docs/e2e/E2E-67-68-grille-dc-et-raccourcis-header.md` — CONFIRMÉ. Clics réels sur les raccourcis d'en-tête et sur le joueur le plus densément entouré de la grille DC.
6. **Regression Guardian** (ce document) : mise à jour de l'entrée existante "Disposition terrain à effectif variable" (partagée avec STORY-38) + nouvelle entrée dédiée aux raccourcis d'en-tête.

## Ce qui a été spécifiquement testé dans ce passage
- **STORY-67 × STORY-38 (régression croisée sur code partagé)** : PVT à 3 joueurs re-testé dans la même session que DC à 5 — même bloc de code (`layouts` dans `courtPlayerPositions()`), aucune régression constatée sur le layout triangle historique.
- **STORY-67 × multi-écrans** : la même sélection de 5 joueurs DC vérifiée sur Match Mode Expert **et** le sélecteur Passe décisive (`renderPlayerSelect()`) — même disposition sur les deux, confirme la propagation via le point de code central.
- **STORY-68 × bandeau GB/alertes existants (STORY-53/63)** : les pastilles `lwb-reopen`/`ahb-reopen` (déjà dans le même groupe flex que `#settings-btn`) continuent de s'afficher correctement à côté des nouveaux raccourcis, sans chevauchement — vérifié visuellement dans le même passage.
- **STORY-68 × STORY-18 (nav horizontale iPhone)** : les 5 onglets de navigation toujours tous présents dans le DOM sur iPhone étroit après l'ajout des raccourcis, vérifié par requête directe (pas seulement visuel).

## Régressions détectées
Aucune.

## Verdict
**RAS — PRÊT POUR MISE EN PRODUCTION** (`sw.js` v112).
