# Regression Guardian — v88 (STORY-39, PDF pagination robuste + corrections de contenu)

## Portée de ce passage
STORY-39 modifie exclusivement `generatePDF()` et ses sous-fonctions internes (`ensurePageSpace` nouvelle, `drawGoalZone` modifiée, `drawPlayerZoneGrid`/`collectShotPlayers`/`drawShotCardsSection` nouvelles, bloc Top 3, découpage en pages Joueurs/Évolution/Carte tir joueur). Aucun fichier hors `app.js` touché, aucune fonction partagée hors PDF modifiée (`gkStats()`, `teamStat()`, `courtPlayerPositions()` consommées en lecture seule). Ciblage du risque plausible plutôt qu'un re-test complet :
- La feature elle-même (déjà couverte en détail par le QA, cf. `docs/qa/QA-39-pdf-pagination-et-corrections.md`) — pas re-testée une 2e fois ici
- Le câblage UI autour de `generatePDF()` (bouton réel, pas seulement l'appel direct de fonction utilisé par le QA) — c'est le risque plausible propre à ce cycle : un refactor interne à une grosse fonction peut casser silencieusement son point d'entrée
- L'entrée historique "Export PDF" de la checklist, restée obsolète depuis plusieurs cycles hors-BMAD (v80-v86 non repris, cf. note existante) — mise à jour à l'occasion de ce premier passage BMAD réel sur le PDF

## Vérifications

| # | Vérification | Résultat |
|---|---|---|
| 1 | Clic réel (souris CDP, pas d'appel direct de fonction) sur `#gen-pdf-btn` depuis l'onglet Stats → PDF, avec un match minimal (1 joueur + 1 GB par équipe, 2 événements) → `generatePDF()` s'exécute sans exception JS, produit un PDF de 5 pages | ✅ `{"saved":true,"pages":5,"error":null}` |
| 2 | `bind()` réattache correctement `pdfBtn.onclick=generatePDF` après un changement de vue déclenché par état (`S.view="stats";S.statsTab="pdf";R()`), pas seulement au chargement initial | ✅ `onclickIsGeneratePDF:true` |
| 3 | STORY-39 elle-même (pagination 6 pages sur effectif 14v14, cas vide 4 pages sans section Carte tir joueur, Top 3 glyphe/cadrés, adversaire+centrage, Zones d'impact cohérence) | ✅ cf. `docs/qa/QA-39-pdf-pagination-et-corrections.md` — repris ici comme référence, pas re-testé une 2e fois |

## Mise à jour de la checklist
Entrée "Export PDF (3 pages)" — obsolète (le PDF n'a plus 3 pages fixes depuis les correctifs hors-cycle v80-v86, jamais mis à jour dans cette checklist) — remplacée par une entrée reflétant l'état réel : pagination dynamique multi-pages (`Page X/N` calculé), couverte pour la première fois par un vrai cycle `/verifie` avec STORY-39.

## Non re-testé dans ce cycle (hors risque plausible)
Le reste de la surface fonctionnelle (saisie Match, terrain, Bilan, Supabase, mode lecteur, etc.) n'est touché par aucun fichier modifié dans STORY-39 — aucune raison de re-tester, conformément au mandat.

## Verdict
**RAS** — aucune régression détectée.
