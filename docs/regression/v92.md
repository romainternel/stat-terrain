# Regression Guardian — v92 (STORY-44 : bloc terrain Comparaison, STORY-46 : correctif échelle points)

## Portée de ce passage
Deux changements livrés ensemble : un ajout net (STORY-44, nouveau bloc dans `renderStatCompare()`) et un correctif transversal touchant 4 sites de rendu existants (STORY-46, mode "points"). Le correctif STORY-46 est le plus risqué des deux — il touche du code déjà en production depuis STORY-22, donc ciblage explicite du risque de régression sur l'interaction existante plutôt qu'un simple re-test de STORY-44 seule.

## Vérifications

| # | Vérification | Résultat |
|---|---|---|
| 1 | `renderStatCompare()` : tableau comparatif + graphique d'évolution du score (juste au-dessus du nouveau bloc) rendus identiques à avant — aucune ligne de ces deux blocs modifiée, seule une nouvelle chaîne insérée après | ✅ lecture de code — `stats`/`goalEvts`/évolution non touchés |
| 2 | "🎯 Tirs par poste" (juste en dessous du nouveau bloc) toujours présent et identique | ✅ `posSvg` non modifié, simplement concaténé après le nouveau bloc |
| 3 | `renderCompareCourt()` : cas 0 événement (équipe sans aucun tir) — pas d'exception, en-tête "0/0" + "0" PB | ✅ testé par appel direct, `S.events=[]` |
| 4 | Sélection d'un tir individuel dans `renderPlayerDetail()` (mode points, `data-pd-shot`) toujours fonctionnelle après le correctif d'échelle | ✅ lecture de code — le binding clic (`document.querySelectorAll("[data-pd-shot]")`, ~ligne 4658) lit l'attribut `data-pd-shot="{i}"` (index dans le tableau `shots`), jamais les coordonnées `cx`/`cy` — le correctif ne change que la position affichée, pas l'attribut utilisé pour la sélection |
| 5 | Mode "zones" partout (Joueurs/Gardiens/Comparaison) rendu à l'identique avant/après le correctif STORY-46 | ✅ capture d'écran zones re-comparée, `shotZoneCourt()`/`buildCourtZones()`/`renderCourtZones()` non touchés par le correctif |
| 6 | Export PDF (`drawCourt()`) non touché par le correctif — déjà correct avant | ✅ lecture de code, aucune ligne du bloc PDF modifiée dans ce cycle |
| 7 | Crosshair de positionnement pendant la saisie en direct (`left:${ap.mapX}%`, CSS, pas SVG) non affecté | ✅ lecture de code, ligne ~1768 non touchée |
| 8 | `.fs-btn` (plein écran) fonctionne sur les 2 nouvelles cartes `renderCompareCourt()`, même binding générique que les autres cartes de l'écran | ✅ classe `.fs-btn` présente, binding global (`document.querySelectorAll(".fs-btn")`) déjà couvert par STORY-30/STORY-43 |
| 9 | `S.events[].x/.y` (donnée stockée) inchangés par le correctif — vérifié qu'aucune écriture n'a été touchée (`clickCourtPosition`, `validateAndClose`) | ✅ lecture de code, seuls les 4 sites de *lecture*/rendu ont été modifiés |

## Non re-testé dans ce cycle (hors risque plausible)
Filtres `S.gkFilter`/`S.gkShotFilter`, calculs `gkStats()`, mode lecteur, persistance `S.shotViewMode` — non touchés par ce cycle, déjà couverts par les entrées STORY-30/STORY-43 de la checklist.

## Mise à jour de la checklist
Nouvelle entrée "Bloc terrain agrégé équipe — Stats → Comparaison" (STORY-44). Entrées existantes "Terrain SVG" (STORY-22) et "Bascule points/zones sur le terrain" (STORY-43) complétées avec le correctif d'échelle du mode "points" (STORY-46).

## Verdict
**RAS** — aucune régression détectée. Changement de rendu volontaire et attendu sur les matchs déjà enregistrés (tirs en mode "points" désormais répartis correctement sur tout le terrain) — pas une régression, cf. `docs/qa/QA-46-bug-echelle-points-tir-svg.md`.
