# Rapport de non-régression — v49 (STORY-02)

*Produit par le Regression Guardian — squad de contrôle BMAD*

## Changement livré dans cette version

`style.css` (condensation équipes/timer + fix barre d'actions pour iPhone portrait) + bump `sw.js` `fenix-stats-v48` → `v49`. Voir `docs/code-review/STORY-02.md` et `docs/qa/QA-02-layout-match-iphone-portrait.md`.

## Analyse de risque de régression

Changement scopé à `@media (max-width:700px) and (orientation:portrait)` — n'affecte ni l'iPad (toute largeur ≥700px), ni l'iPhone en paysage (hors de cette condition, cf. STORY-03 séparée), ni aucun autre écran que Match (les classes touchées — `.ml-team`, `.ml-timer`, `.mlt-poss-btn`, `.ml-actions`, `.act-h*` — sont spécifiques à l'écran Match).

## Ce qui a été ciblé (plausible à risque)

- **Écran Match iPad** (paysage et portrait) : capture `14-story02-ipad-landscape-noregress.png` comparée à la référence — identique. RAS.
- **Navigation header sur iPhone** (STORY-18, livrée juste avant) : non re-testée explicitement dans cette passe, mais aucune classe partagée entre les deux stories (`.nav`/`.logo` vs `.ml-*`/`.act-h`) — risque de collision jugé nul.
- **Barre d'actions elle-même** (le cœur du changement) : re-testée et mesurée (voir QA-02). PASSED.

## Ce qui n'a pas été retesté en profondeur (test de fumée, pas de raison de suspicion)

- Saisie d'action (workflow complet), alertes automatiques, stats GB, export PDF, import/export CSV, mode hors-ligne : `app.js` non modifié par cette story, aucune raison de re-tester en profondeur.

## Trouvaille annexe (pas une régression de cette version, mais à ne pas oublier)

Chevauchement des étiquettes joueurs sur le terrain (`.cp-player`) à largeur réduite avec un effectif complet — confirmé **préexistant** (présent avant cette story, indépendant du changement livré ici, non introduit par v49). Documenté dans `docs/qa/QA-02-layout-match-iphone-portrait.md` et la checklist. Recommandation : le Scrum Master devrait le transformer en story dédiée avant qu'il ne soit oublié.

## Verdict

**RAS** — aucune régression détectée sur ce qui a été ciblé.

## Mise à jour de la checklist

`docs/regression/checklist.md` : ligne "Layout Match iPad" reconfirmée, "Layout Match iPhone" scindée en portrait (✅ STORY-02) et paysage (toujours ❌, STORY-03 à venir), note ajoutée sur la trouvaille `.cp-player`.
