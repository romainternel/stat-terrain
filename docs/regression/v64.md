# Regression Guardian — passage avant mise en production v64

## Changement de cette version
STORY-12 : synchronisation sortante (outbox). Touche 6 points de création/édition d'événement (`clickTeam` TM, `recordTM`, `validateActionPanel`×2, `validateAndClose`×2, `recordEvent`) + `undoLast()`, ainsi que `gid()` (UUID v4 au lieu du format historique) et `DB_VER` (nouveau store IndexedDB). Surface touchée large → vérification approfondie, pas un test de fumée.

## Vérification approfondie
| Vérification | Résultat |
|---|---|
| BUT complet (terrain+zone), Expert | ✅ — id UUID valide, score correct |
| TM | ✅ — événement créé correctement |
| Annuler (undo) | ✅ — retire bien l'événement (2→1), et de la file de sync locale |
| Mode Simple (STORY-24, `recordEvent` réutilisée) | ✅ — score correct après clic, id UUID valide |
| Format d'id (`gid()`) changé en UUID v4 | ✅ — vérifié qu'aucun code ne parsait l'ancien format ("id"+compteur) avant de changer, pas de régression cachée |
| Nouveau store IndexedDB (`pendingSync`, `DB_VER` 1→2) | ✅ — `onupgradeneeded` crée le nouveau store sans toucher au store `matches` existant, testé : historique des matchs toujours accessible |

## Features non re-testées (hors risque plausible)
Export PDF, import/export CSV, Stats/Bilan — aucun lien avec la création d'événements elle-même (lisent `S.events` déjà construit, pas affectés par le format d'id ou l'ajout de sync).

## Point non couvert (limite connue, documentée par le QA/Developer)
Envoi réussi contre le vrai projet Supabase — nécessite une vraie session, à vérifier par Romain. Suppression propagée non re-tentée si hors-ligne au moment de l'annulation — limite assumée, pas une régression de cette version.

## Verdict
**RAS** — aucune régression détectée malgré la surface touchée large (6 points d'entrée + changement de format d'id).
