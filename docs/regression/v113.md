# Regression Guardian — v113 (2026-08-20)

## Contexte
Passage de non-régression pour STORY-69 (3 correctifs de bugs Majeurs identifiés lors de l'audit `/verifie-complet` du même jour). Code Review APPROUVÉ (après une reprise), QA PASSED, E2E CONFIRMÉ. `sw.js` bumped v112→v113.

## Périmètre ciblé
Conformément au mandat (cibler ce qui est plausiblement à risque, smoke-test le reste) :
- `loadMatchAsCurrent()` — utilisée par 2 points d'entrée : "📂 Charger" (Matchs) déjà testé en QA/E2E, **raccourci PDF de Bilan (STORY-36) testé séparément par moi-même** puisque c'est un chemin d'appel différent de la même fonction modifiée, jamais explicitement re-testé par le QA/E2E de cette story
- `markMatchFinishedById()` — signature étendue (paramètre `meta` optionnel) ; les 3 autres appelants (`switchTeamProfile()`, `saveMatch()`, `discardResumableMatch()`) vérifiés par lecture de code (Code Review) : aucun ne passe `meta`, donc aucun changement de comportement pour eux (valeur par défaut = lecture live de `S.*`, identique à avant) — non re-testés en direct, risque jugé négligeable (changement additif, pas de branche de code existante modifiée)
- `stopTimer()` — retourne désormais une promesse au lieu de rien ; 3 autres appelants (`t-toggle`, `per-btn`, `resetTimer()`) ignorent tous la valeur de retour — non re-testés en direct, changement strictement additif

## Vérifications effectuées

| Vérification | Méthode | Résultat |
|---|---|---|
| "📂 Charger" (Matchs) n'écrase plus l'effectif local | QA + E2E, clic réel, avant/après `localStorage` | ✅ |
| Raccourci PDF Bilan n'écrase plus l'effectif local | Smoke-test dédié (moi-même) — même effectif réel (22 joueurs) chargé une fois via "✓ Tout", vérifié intact après le raccourci PDF, PDF généré avec succès (téléchargement confirmé, 0 erreur console) | ✅ |
| Bouton PD absent après un événement non-but | QA + E2E, 2 sessions indépendantes, captures d'écran | ✅ |
| Bouton PD présent et fonctionnel après un vrai but (non-régression) | QA + E2E, assist réellement attribué et vérifié dans l'événement | ✅ |
| "Nouveau match" — reset local jamais bloqué par le réseau | QA + E2E, réseau simulé à 3-3,5s, 2 sessions indépendantes | ✅ |
| "Nouveau match" — plus de match fantôme "in_progress" | QA + E2E, vérifié directement sur Supabase après résolution du réseau différé, 2 sessions indépendantes | ✅ |
| "Nouveau match" — season/journée pas contaminées par le match suivant | QA + E2E, `journee` vérifiée correcte sur le match fini (pas incrémentée) | ✅ |
| Aucune erreur console inattendue sur l'ensemble des 3 sessions de test (QA + E2E + smoke Regression Guardian) | Vérifié à chaque étape | ✅ 0 erreur |

## Nettoyage de production

Root cause du match résiduel "Yoshi" trouvé en tout début d'audit (2026-08-20) confirmée par cette story : condition de course de `newMatch()`, désormais corrigée. Les deux enregistrements Supabase liés à "Yoshi" (un `status:'in_progress'` créé le 15:42, un `status:'finished'` créé le 15:41 juste avant) ont été supprimés, ainsi que tous les matchs de test créés pendant les 3 sessions QA/E2E/Regression Guardian de cette story (4 matchs de test au total, tous `status:'finished'` avant suppression — jamais restés fantômes, preuve supplémentaire que le correctif tient). **Seuls les 2 vrais matchs de Romain (Rodez ×2) subsistent sur Supabase**, vérifié par un dernier inventaire complet de la table `matches` (team_profile=cf) après nettoyage.

Aucun test n'a été exécuté contre le site de production `romainternel.github.io/stat-terrain/` (le code corrigé n'y est pas encore déployé au moment de ce passage) — tout a été testé via un serveur local (`http-server`) servant les fichiers du dépôt, contre le vrai backend Supabase de production (mêmes identifiants).

## Verdict

**RAS** — les 3 correctifs sont sains, aucune régression détectée sur les parcours adjacents vérifiés. **Prêt pour mise en production** (déploiement : commit + push, non encore fait à la fin de ce passage — cf. rapport final).
